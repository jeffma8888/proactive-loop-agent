# Enhancement Roadmap

A maturity/enhancement backlog layered on the shipped **v0.1.1**, grounded in
[`SPEC.md`](SPEC.md). The product is already complete and tested (134 tests
green through iter-02); everything here is a small, self-contained increment
that raises the bar of an existing layer — L2 scout perception, L1 loop
execution, L0 resilience/observability, or CLI/DX — without breaking the public
contracts. Each iteration of the dev loop selects exactly one row.

Ordering is by leverage on the product's thesis ("a proactivity layer that is
resilient by design"), balanced against blast radius. Seeded from both PM scouts
(new-capability lens + hardening/DX lens) across iters 01–03 and a fresh read of
`SPEC.md`. Net-new rows (#17+) surfaced mid-loop by a scout may jump the queued
backlog when they close a docs-vs-code honesty gap on a public repo (the standing
iter-01 tie-break rule: an integrity fix outranks a net-new capability). Applied
iters 01, 03, and 05; iters 04, 06, and 07 went net-new (`pla runs`, `pla explain`,
then `pla trace`) because no live 30s-falsifiable docs-vs-code contradiction existed
those iterations. iter-08 returns to the queued backlog, applying #6 (retry telemetry)
to complete the observability arc runs → explain → trace → retry-visibility. iter-09
shipped #5 (DependencyCollector), iter-10 shipped #21 (net-new `--workspace` guard), and
iter-11 returns to the queued backlog with #8 (WorkingTreeCollector) — its consecutive-collector
objection now spent by iter-10's CLI verb. The integrity tie-break is now a depleted resource: both iter-07
scouts independently re-verified the verb lists and version sources are all consistent,
| # | Enhancement | Layer | Value | Risk | Source | Status |
|---|-------------|-------|-------|------|--------|--------|

> **This file is an INDEX, deliberately kept small.** It grew to 200KB of settled history, which pushed the PM stage past the agent CLI's hard 600-second timeout and stalled the loop entirely (iteration 90 lost all four attempts, producing nothing).
>
> Full rationale for every row -- the complete status prose and the iteration-by-iteration narrative -- is preserved verbatim in [`ROADMAP_ARCHIVE.md`](ROADMAP_ARCHIVE.md). Read that file ONLY when you need one specific row's detail; never read it wholesale.
>
> **Maintainers (including automated contributors): keep rows one line and terse.** Put a new row's full reasoning in your iteration's `pm.md`, not here. If this file passes ~40KB again the loop will stall the same way.

| 1 | **Retry-wrap the L2 `synthesize` call** so `pla scan`/`pla run` survive a transient throttle/timeout the same way a dispatched goal does | L0×L2 | High | Low | Scout B C1 (iter-01) | **SHIPPED — iter-01** (`172d38f`) |
| 2 | **Top-level CLI error boundary in `main()`** — map foreseeable operator/environment faults (persistent throttle, bad `--provider`, missing/malformed scripted file, corrupted slate ... | CLI/L0 | High | Low | Scout B C1 (iter-02) | **SHIPPED — iter-02** (`9d638a9`) |
| 3 | `pla explain --slate --goal-id` — print the score arithmetic + which gate rule fired + sources, making the autonomy contract auditable | CLI/L2 | High | Low | Scout A C1 (iter-02) | **SHIPPED — iter-06** (`5d59292`; completes the scan → runs → explain legibility arc; additive ... |
| 4 | GitHub Actions CI (`uv sync` + `uv run pytest` + `make demo`) + a "tests passing" README badge — first trust signal on a public repo; machine-verifies every future loop commit | DX/CI | High | Very Low | Scout B C2 (iter-02) | **SHIPPED — human, out-of-band (2026-08-04) |
| 5 | `DependencyCollector` — new L2 signal reading project manifests (`pyproject.toml`/`package.json`/…) so the scout proposes stack-appropriate goals | L2 | High | Low | Scout A C1 (iter-01) | **SHIPPED — iter-09** (`d0da144`; new `kind="dependency"` flows into synthesis via `by_kind() |
| 6 | Surface L0 retry telemetry into `RunState` + the run summary + `trace` header (wire the defined-but-unused `on_retry` hook) so self-healing is visible | L0×L1 | Med-High | Low | Scout B C2 (iter-01) | **SHIPPED — iter-08** (`807b819`; scalar `RunState.retries` counter, executor wires `on_retry`; ... |
| 7 | `pla --version` + a version-consistency guard test; also fix the stale `SPEC.md §2` `__version__ = "0.1.0"` comment (code is `0.1.1`) | CLI/DX | Med | Very Low | Scout B C3 (iter-02) | **SHIPPED — iter-05** (`169f1a1`; integrity tie-break: only live 30s-falsifiable docs-vs-code g ... |
| 8 | `WorkingTreeCollector` — new L2 signal for uncommitted/unpushed git work (`git status --porcelain`, `@{u}..HEAD`); complements `git_activity` (present vs. past) | L2 | Med | Low | Scout A C2 (iter-02) | **SELECTED — iter-11** (adds present-state git perception — the product's core scan-the-working ... |
| 9 | Read-only `search_files(query, path)` sandbox tool for the L1 loop so it can *discover* code in a real workspace, not just read known paths | L1 | Med | Low-Med | Scout A C2 (iter-01) | **SELECTED — iter-13** (opens the L1 ACT sandbox — the only architectural layer untouched since ... |
| 10 | `append_file(path, content)` sandbox tool — first-class incremental artifact authoring across loop iterations (vs. read-then-rewrite) | L1 | Med | Low | Scout A C3 (iter-02) | **SHIPPED — iter-17** (`79dd40f`; Scout A's RECOMMENDED A1; genuinely-new L1 incremental-author ... |
| 11 | `pla scan --format {table,json,markdown}` — pipeable/shareable slate output (default `table`, backward compatible) | CLI/DX | Med | Very Low | Scout A C3 (iter-01) | **SELECTED — iter-12** (default `table` keeps every existing scan byte-stable; `json` = one JSO ... |
| 12 | Collectors robustness audit — make `TestGracefulDegradation` registry-driven (drive `@parametrize` from `all_collectors()` so it can't rot) + add hostile-input cases, proving the S ... | L2/quality | Low-Med | Very Low | Scout B B2 (iter-19) | **SELECTED — iter-31** (Scout B's RECOMMENDED B1; registry now returns **9** collectors [count ... |
| 13 | Wire the already-declared `pytest-cov` into a `make cov` target + `[tool.coverage]` config (dead dev-dep → visible quality signal) | DX | Low-Med | Very Low | Scout B C3 (iter-01) | **SELECTED — iter-52** (Scout B's RECOMMENDED B1; makes the declared-but-unwired `pytest-cov` d ... |
| 14 | Ship a PEP 561 `src/proactive_loop/py.typed` marker so the fully type-hinted library exports as typed to downstream consumers | DX | Low | Very Low | Scout B C3 sibling | **SELECTED — iter-58** (Scout B's RECOMMENDED B1; ships the empty PEP 561 marker at `src/proact ... |
| 15 | `pla scan --top N` to cap the printed slate for large workspaces (JSON write unchanged) | CLI/DX | Low | Very Low | PM-lead (SPEC read) | **SELECTED — iter-27** (Scout B's RECOMMENDED B1; caps the STDOUT-rendered slate to the top-N r ... |
| 16 | README section documenting the lazy-imported provider adapters (anthropic/openai/bedrock) with an env-var example | Docs | Low | None | PM-lead (SPEC read) | **SELECTED — iter-55** (Scout B RECOMMENDED B1; BROADENED to the full `PLA_*` env-var reference ... |
| 17 | **Wire `PLA_RETRY_*` env vars into `Settings.from_env`** — the five `RetryPolicy` knobs (max_attempts, base_backoff_sec, backoff_factor, max_backoff_sec, jitter_frac) are the produ ... | L0/config | High | Very Low | Scout B C1 (iter-03) | **SHIPPED — iter-03** (`d4fb593`; net-new, jumped queue on integrity grounds) |
| 18 | **`pla runs` — list & inspect past dispatched runs** (read-only CLI verb over `state_dir/run-*`; loads each `Checkpoint` + `meta.json`, prints a table of run id · status · iteratio ... | CLI/L0×L1 | High | Low | Scout A C1 (iter-04) | **SHIPPED — iter-04** (`8240ce4`) |
| 19 | **Adversarial regression suite for the ACT sandbox** — prove the documented `_within` symlink-escape defense in `tools.py` (currently zero symlink tests) + `list_files` traversal/a ... | L1/security | High | Very Low | Scout B C1 (iter-04) | **SELECTED — iter-35** (Scout B's RECOMMENDED B1; the write-path `_within` gate on `write_file` ... |
| 20 | **`pla trace --run-dir DIR [--json]`** — render one dispatched run's persisted PLAN→ACT→CHECK transcript (`RunState.steps`) from its checkpoint. Read-only, LLM-free, `cli.py`-only; ... | CLI/L1×L0 | High | Low | Scout A C1 (iter-07) | **SELECTED — iter-07** (net-new; picked over the strong test-only sandbox-hardening alternative ... |
| 21 | **Validate `--workspace` in `pla scan` / `pla run`** — a missing or non-directory path fails fast with `error: workspace not found: <path>` on stderr + exit **2**, instead of silen ... | CLI/hardening | Med | Very Low | Scout B C1 (iter-10) | **SELECTED — iter-10** (front-door correctness fix on the two most-used verbs + highest-certain ... |
| 22 | **Range-guard `auto_dispatch_min_score`** (`Field(default=4.0, ge=0.0)`) — the single numeric `Settings` knob with no bound, and it governs the L2 autonomy gate: a negative thresho ... | L2/config | Med-High | Very Low | Scout B C1 (iter-12) | **SELECTED — iter-14** (`Field(default=4.0, ge=0.0) |
| 23 | **`pla signals --workspace W [--json] [--kind K]`** -- read-only, LLM-free perception inspector that prints the raw `ContextSignal`s the collectors emit, grouped by kind (human) or ... | CLI/L2 | High | Very Low | Scout A A1 (iter-14/15) | **SELECTED -- iter-15** (fully offline-verifiable, additive, zero-blast-radius cli.py+docs; reu ... |
| 24 | **`TestPostureCollector`** -- new L2 collector (`kind="test_posture"`) emitting one signal per project dir (root + each direct child) reporting `(src, test)` file counts and flaggi ... | L2 | High | Very Low | Scout A A1 (iter-14/15/16) | **SELECTED -- iter-16** (new *perception* = the product thesis; picked over Scout B's #13 `make ... |
| 25 | **`pla watch --workspace W [--interval S] [--max-scans N]`** -- new CLI verb that repeatedly runs the existing scan pipeline every `--interval` seconds, wiring the shipped-but-unre ... | CLI/scheduler | High | Low-Med | Scout A A1 (iter-17/18) | **SELECTED -- iter-18** (the product's NAMESAKE proactive-scan-on-a-schedule capability, provab ... |

| 26 | **Isolate a raising collector at the `_collect` boundary** -- wrap each `collector.collect()` in `_collect` (the orchestration seam behind `scan`/`run`/`signals`/`watch`) in a `try ... | CLI/L2/resilience | High | Low | Scout B B1 (iter-18/19) | **SELECTED -- iter-19** (applies the product's headline "resilient by design" thesis to the one ... |
| 27 | **`GitStateCollector`** (`kind="git_state"`) -- new L2 collector surfacing INTERRUPTED/dangling git operations (unfinished merge via `.git/MERGE_HEAD`, rebase via `.git/rebase-merg ... | L2 | High | Low | Scout A A1 (iter-19/20) | **SELECTED -- iter-20** (new *perception* = the product thesis; its ONE blocker cleared when it ... |
| 28 | **`find_files(pattern, path=".")`** — read-only L1 ACT-sandbox tool doing recursive **basename glob discovery** (stdlib `fnmatch`, case-folded both sides), so a dispatched loop can ... | L1 | Med-High | Low | Scout A A1 (iter-21) | **SELECTED — iter-21** (new *executable* capability on the coldest productive layer [L1, 4 iter ... |
| 29 | **`pla explain --slate S --goal-id ID --json`** -- machine-readable form of the read-only, LLM-free autonomy-gate audit: one JSON object of exactly 12 explicit keys (`id, title, ca ... | CLI/L2 | High | Very Low | Scout B B1 (iter-22) | **SELECTED -- iter-22** (completes the machine-readable-inspection arc across the WHOLE transpa ... |
| 30 | **Actionable "SDK not installed" error for live providers** -- each live `_create_*` in `llm/providers.py` (anthropic/openai/bedrock) guards its lazy SDK import so a selected-but-u ... | L0/providers | Med-High | Very Low | Scout B B1 (iter-23) | **SELECTED -- iter-23** (closes iter-02's error-boundary contract for the one foreseeable envir ... |
| 31 | **`pla diff --old A.json --new B.json [--json]`** -- read-only, LLM-free slate-delta inspector: match goals across two saved slates by **normalized title** (`title.strip().lower()` ... | CLI/L2 | High | Low | Scout A A1 (iter-20-24) | **SELECTED -- iter-24** (the FOUR-times-deferred top net-new backlog row [named top pick at ite ... |
| 32 | **`pla -v/--verbose` runtime log verbosity** -- add a `count`-action `-v`/`-vv` to the shared globals parent parser; in `main()` (before dispatch) map count -> level (`0`->WARNING ... | CLI/L0 | Med-High | Low | Scout B B1 (iter-24/25) | **SELECTED -- iter-25** (makes the product's NAMESAKE 'resilient by design' L0 story visible at ... |
| 33 | **`stat_file(path)` L1 sandbox tool** -- the 'describe-one-path' member of the discovery quartet (find/list/grep/**describe**/read): deterministic one-line observation `type=<file/ ... | L1 | Med | Very Low | Scout A A1/A2 (iter-23/24/25/26) | **SELECTED -- iter-26** (Scout A's RECOMMENDED A1, thrice-vetted lead-Planned filler; opens the ... |
| 34 | **`CiConfigCollector`** (`kind="ci_config"`) -- new L2 collector emitting one signal per top-level project dir reporting whether it carries a recognized CI config (`.github/workflo ... | L2 | Med | Low | Scout A A3 (iter-24) | **SHIPPED -- factory iter 63** (`c702824`; new L2 `kind="ci_config"` collector completing the d ... |
| 35 | **`MergeConflictCollector`** (`kind="merge_conflict"`) -- new L2 collector perceiving unresolved VCS conflict markers that survived INTO committed files: line-scans scanned-ext fil ... | L2 | High | Low | Scout A A1 (iter-26/27/28) | **SELECTED -- iter-28** (Scout A's RECOMMENDED A1; a genuine PERCEPTION HOLE invisible to all t ... |
| 36 | **`head_file(path, max_lines=40)`** L1 sandbox tool -- the bounded windowed reader completing the discovery family **find / list / grep / describe(stat) / PEEK(head) / read(full)** ... | L1 | Med | Low | Scout A A1 (iter-27/29) | **SELECTED -- iter-29** (Scout A's RECOMMENDED A1; the ONLY candidate on a productive layer nei ... |
| 37 | **Actionable env-var coercion errors in `Settings.from_env`** -- a mistyped numeric `PLA_*` value (the three scalar knobs `AUTO_DISPATCH_MIN_SCORE`/`MAX_ITERATIONS`/`MAX_LLM_CALLS` ... | L0/config | Med-High | Very Low | Scout B B1 (iter-30) | **SELECTED -- iter-30** (applies the product's OWN iter-23 principle -- 'misconfiguration shoul ... |
| 38 | **`ollama` local-model provider** -- add `"ollama"` to `VALID_PROVIDERS` + an `_create_ollama` branch in `create_client` (`llm/providers.py`) so the plan->act->check loop can run a ... | L0/providers | Med-High | Low | Scout A A3 (iter-18/32) | **SELECTED -- iter-32** (extends the product's headline OFFLINE-FIRST thesis from tests/demo to ... |
| 39 | **`remove_file(path)` L1 sandbox tool** -- the FIRST destructive-mutation tool in the ACT sandbox: deletes ONE file under `artifacts_dir` ONLY (never `workspace_root`), completing ... | L1/security | Med | Low-Med | Scout A A1 (iter-33) | **SELECTED -- iter-33** (Scout A's RECOMMENDED A1; a genuinely-new CAPABILITY CATEGORY -- mutat ... |
| 40 | **`pla scan --format csv`** -- add a `csv` choice to the `scan --format` enum emitting the ranked, gated slate as an RFC-4180 CSV data stream (stdlib `csv` module, `QUOTE_MINIMAL`) ... | CLI/DX | Med | Very Low | Scout A A1 (iter-34) | **SHIPPED -- iter-34** (`b64c63c`; Scout A's RECOMMENDED A1; lands on the COLDEST productive su ... |
| 41 | **Validate the scripted-responses file shape at load time** -- harden `ScriptedLLMClient` so a malformed offline scripted-responses JSON file fails fast with one clean `error:` lin ... | L0/DX/robustness | Med-High | Low | Scout B B1 (iter-36) | **SHIPPED -- iter-36** (`a4f4487`; Scout B's RECOMMENDED B1; a VERIFIED contract violation -- r ... |
| 42 | **`LargeFileCollector`** (`kind="large_file"`) -- new L2 collector flagging files at/above a byte-size threshold (default `min_bytes=5_000_000`, ctor-overridable), giving the scout ... | L2 | Med | Low | Scout A A2 (iter-37) | **SHIPPED -- iter-37** (`c747689`; Scout A's A2, chosen over its own RECOMMENDED A1 `LockfileDr ... |
| 43 | **Parse-time validation of `pla watch --interval` / `--max-scans`** -- the LAST two front-door numeric args not in the CLI's exit-2 guard family (siblings `--top`/iter-27, `--works ... | CLI/hardening | Med-High | Very Low | Scout B B1 (iter-38) | **SHIPPED -- iter-38** (`7a50029`; Scout B's RECOMMENDED B1; live-reproduced 3 ways; closes the ... |

| 44 | **`pla policy [--json]`** -- new read-only, LLM-free, zero-input CLI verb printing the STANDING autonomy contract: the four ordered `policy.gate` rules (first match wins), the reso ... | CLI/L2 | High | Very Low | Scout A A1 (iter-39) | **SHIPPED -- iter-39** (`1b017aa`; Scout A's RECOMMENDED A1; surfaces the product's HEADLINE sa ... |
| 45 | **`pla collectors [--json]`** -- read-only, LLM-free, zero-input verb listing every registered collector + a one-line description of what it perceives; the missing FRONT DOOR of th ... | CLI/L2 | Med-High | Very Low | Scout A A2 (iter-38/39) | **SELECTED -- iter-57** (Scout A's RECOMMENDED A1; the L2-perception FRONT DOOR of the transpar ... |

| 46 | **Reject non-finite `pla watch --interval`** (`nan`/`inf`/`-inf`) at parse time -- harden the iter-38 `_non_negative_float` validator, which guards only `value < 0.0` so `nan`/`inf ... | CLI/L0 | High | Very Low | Scout B B1 (iter-40) | **SHIPPED -- iter-40** (`7dd6717`; live-reproduced defect INSIDE a shipped guard; the loop's st ... |
| 47 | **Reject non-finite `Settings`/`RetryPolicy` config floats** -- `PLA_AUTO_DISPATCH_MIN_SCORE=inf` is silently ACCEPTED (`inf >= 0.0` is True) and disables the headline `pla run` au ... | L0/config | Med-High | Low-Med | Scout B B2 (iter-40) | **SHIPPED -- iter-41** (`cbb2a33`; Scout B's RECOMMENDED B1; the config-side `+inf` mirror of t ... |

| 48 | **`SecretFileCollector`** (`kind="secret_file"`) -- new L2 security-hygiene collector flagging **secret-shaped files by BASENAME only** (never by content): exact names `.env`/`.env ... | L2 | High | Low | Scout A A1 (iter-41/42) | **SHIPPED -- iter-42** (`cff5da2`; Scout A's RECOMMENDED A1; a genuinely-NEW *perception* axis ... |

| 49 | **Pre-flight validation of output-path CLI args (`scan --out`, `run`/`dispatch` `--state-dir`)** -- fail fast with `error: <msg>` on stderr + exit **2 BEFORE any client build / col ... | CLI/L0 | High | Very Low | Scout B B1 (iter-43) | **SHIPPED -- iter-44** (`62398d9`; RE-SELECT of iter-43's pick: this is a live-reproduced tier- ... |

| 50 | **`move_file(src, dst)` L1 ACT-sandbox tool** -- atomic rename/relocate of one artifact under `artifacts_dir` ONLY (never `workspace_root`), completing the mutation family create(` ... | L1 | Med | Low | Scout A A1 (iter-43/45) | **SHIPPED -- iter-45** (`8bb2f7f`; Scout A's RECOMMENDED A1; the L1 ACT sandbox is now the COLD ... |

| 51 | **Sanitize the leaked pydantic `ValidationError` on corrupt slate/checkpoint load** -- map the raw multi-line pydantic dump on the slate-load seam (`GoalSlate.model_validate_json` ... | CLI/L0/DX | Med-High | Low | Scout B B1 (iter-42/43/46) | **SELECTED -- iter-46** (Scout B's RECOMMENDED B1; the LAST error-presentation gap on a PUBLIC ... |

| 52 | **Pin the `ScriptedLLMClient.complete()` tag-matching contract** -- add a focused, fully-offline behavior test (`tests/test_iter47_behavior.py`) proving the runtime matching / fail ... | L0/quality | Med-High | Very Low | Scout B B1 (iter-47) | **SELECTED -- iter-47** (Scout B's RECOMMENDED B1; PROVES the SPEC S3 matching invariant the co ... |

| 53 | **`pla tools [--json]`** -- new read-only, LLM-free, zero-input CLI verb cataloging the L1 ACT-sandbox tool surface: every registered tool + a one-line description + its access cla ... | CLI/L1 | Med-High | Very Low | Scout A A1 (iter-48) | **SHIPPED -- iter-48** (`63192e8`; Scout A's RECOMMENDED A1; the only verb exposing the L1 ACT ... |
| 54 | **Pin the `_SdkAdapter` retry-taxonomy seam** (`llm/providers.py:138-143`) with a fully-offline behavior test: `throttle_excs -> LLMThrottleError`, `timeout_excs -> LLMTimeoutError ... | L0/quality | Med | Very Low | Scout B B1 (iter-48) | **SELECTED — iter-59** (Scout B's RECOMMENDED B1; the deferral clock DRAINED — deferred iters 4 ... |

| 55 | **`groq` cloud provider** -- add `"groq"` (appended last) to `VALID_PROVIDERS` + a `_create_groq` branch in `create_client` (`llm/providers.py`) so the full plan->act->check loop r ... | L0/providers | Med-High | Very Low | Scout A A1 (iter-49) | **SELECTED -- iter-49** (providers = the single COLDEST layer, 17 iters cold since `ollama`/ite ... |
| 56 | **Pin the `parse_json_block` branch matrix** -- add a fully-offline test-only pin of the crown-jewel LLM-output parser's full decode branch matrix, closing the wholly-untested FENC ... | LLM/quality | Med-High | Very Low | Scout B B1 (iter-49) | **SELECTED — iter-50** (Scout B's RECOMMENDED B1; closes the single most load-bearing untested ... |

| 57 | **Correct the stale `parse_json_block` docstring ordering** -- the "Strategy, in order of preference" list in the `parse_json_block` docstring (`llm/client.py`) claims step 1 is "a ... | Docs/integrity | Low-Med | Very Low | PM-lead (iter-50 spec read) | **SELECTED — iter-51** (Scout B's RECOMMENDED B1; the standing iter-01 tie-break rule — a live ... |

| 58 | **`GitStashCollector`** (`kind="git_stash"`) -- new L2 collector surfacing forgotten `git stash` entries by reading the stash reflog `.git/logs/refs/stash` with **`pathlib` only** ... | L2 | Med-High | Low | Scout A A1 (iter-53) | **SELECTED -- iter-53** (Scout A's RECOMMENDED A1; genuinely-NEW perception on the single COLDE ... |

| 59 | **`tail_file(path, max_lines=40)`** L1 ACT-sandbox tool -- a bounded BOTTOM-of-file peek, the mirror of iter-29's `head_file`, completing the sandbox's top/bottom PEEK PAIR. Return ... | L1 | Med-High | Low | Scout A A1 (iter-54) | **SELECTED -- iter-54** (Scout A's RECOMMENDED A1; genuinely-NEW *executable* capability on the ... |

| 60 | **`pla scan --format html`** -- fifth `--format` choice rendering the ranked, gated slate as ONE self-contained, dependency-free HTML document on stdout (`pla scan --workspace W -- ... | CLI/DX | Med | Very Low | Scout A A1 (iter-54/55/56) | **SELECTED -- iter-56** (Scout A's RECOMMENDED A1; genuinely-NEW capability on the single COLDE ... |

| 61 | **Wire the L2 synthesizer's `on_retry` logging hook** -- give `GoalSynthesizer.synthesize()` an `on_retry=_count_retry` hook so a recovered throttle/timeout on the `pla scan`/`pla ... | L0xL2/observability | Med-High | Very Low | Scout B B1 (iter-60) | **SHIPPED -- iter-60** (`0699c67`; Scout B's RECOMMENDED B1; picked over Scout A's RECOMMENDED ... |

| 62 | **`diff_files(path_a, path_b)`** L1 ACT-sandbox tool -- a read-only, bounded, stdlib-`difflib`-only unified diff of two sandbox files, completing the read family as read / peek-top ... | L1 | Med-High | Low | Scout A A1 (iter-54/56/60/61) | **SHIPPED -- iter-61** (`a1983ae`; Scout A's RECOMMENDED A1 and the PM-lead's own explicitly-qu ... |

| 63 | **`pla run --dry-run`** -- add a `--dry-run` flag to the product's ONLY autonomous-action verb that runs the full scan+gate+slate-write path and PREVIEWS the single goal `run` WOUL ... | CLI/Safety | Med-High | Low | Scout A A1 (iter-62) | **SELECTED -- iter-62** (Scout A's RECOMMENDED A1; picked over Scout B's RECOMMENDED B1 [name-t ... |

| 64 | **Exercise the five live-provider `_complete` response adapters with in-memory SDK stubs** -- call `create_client(Settings(provider=<p>)).complete(...)` for `anthropic`/`openai`/`b ... | quality/test | High | Very Low | Scout B B1 (iter-64) | **SELECTED -- iter-64** (Scout B's RECOMMENDED B1; picked over Scout A's RECOMMENDED A1 `copy_f ... |

| 65 | **`together` cloud provider** -- add `"together"` (appended LAST) to `VALID_PROVIDERS` + a `_create_together` branch in `create_client` (`llm/providers.py`) so the full plan->act-> ... | L0/providers | High | Low | Scout A A1 (iter-65) | **SELECTED -- iter-65** (Scout A's RECOMMENDED A1; picked over Scout B's RECOMMENDED B1 [`git_a ... |

| 66 | **`replace_in_file(path, old, new)`** L1 ACT-sandbox tool -- the missing in-place EDIT primitive: substitute EVERY literal occurrence of `old` with `new` inside an existing artifac ... | L1 | Med-High | Low | Scout A A1 (iter-66) | **SELECTED -- iter-66** (highest thesis advance -- a genuinely-new, reviewer-visible capability ... |

| 67 | **Pin `git_activity.py`'s degradation + parse-robustness branches** (raise the repo's lowest-covered module from 82% toward 100%) -- fully-offline, monkeypatch `git_activity.subpro ... | L2/quality | Med-High | Very Low | Scout B B1 (iter-66) | **SELECTED -- iter-74** (the ONLY genuinely-open queued frontier row, now taken; ships the full ... |

| 68 | **Fix the two stale SPEC catalog counts + drift-guard them** -- `SPEC.md` `pla tools --json` says 'array of **10** objects' (live 12->13 after #66) and `pla collectors --json` says ... | Docs/quality | Low-Med | Very Low | Scout B B3 (iter-66) | **SELECTED -- iter-67** (the standing iter-01 tie-break -- a live docs-vs-code integrity fix ou ... |

| 69 | **Emit a live WARNING when the L1 loop's fail-safe absorbs a malformed PLAN or CHECK** -- close the degradation-observability gap the L0-retry arc (iters 08/25/60) already set. Tod ... | L1/observability | Med-High | Low | Scout B B1 (iter-67) | **SELECTED -- iter-68** (Scout-B's RECOMMENDED B1, the blessed/queued NEXT pick from iter-67; p ... |

| 70 | **Persist a `parse_errors` counter on `RunState` + surface it in the run summary and `trace` header** -- the AUDITABLE half of iter-68's live fail-safe WARNING, mirroring EXACTLY h ... | L1/observability | Med-High | Low | Scout B B2 (iter-68) | **SELECTED -- iter-69** (Scout-B's RECOMMENDED B1, the ROADMAP's own "land next": CLOSES iter-6 ... |

| 71 | **`pla scan --collector NAME` allowlist flag** -- a repeatable `--collector` flag on `pla scan` restricting WHICH collectors feed synthesis (`choices=` the live collector names, so ... | CLI/L2 | High | Low-Med | Scout A A1 (iter-68/69) | **SELECTED -- iter-73** (the queued top net-new pick, now taken. Adds a repeatable, registry-va ... |
| 72 | **Expose the persisted resilience counters in `runs --json`** -- add `retries` and `parse_errors` to `_run_row` (feeding `pla runs --json`) so a CI/monitoring script can flag a thr ... | CLI/observability | Med | Very Low | Scout B B3 (iter-69) + #70 follow-on | **SELECTED -- iter-71** (Scout-B's RECOMMENDED C1 = this queued row, now UNBLOCKED and diversif ... |

| 73 | **`LockfileDriftCollector`** (`kind="lockfile_drift"`) -- the FIRST *relational* L2 collector: pairs each dependency manifest with its sibling lockfile and emits a fact when the lo ... | L2 | High | Low-Med | Scout A C1 (iter-70) | **SELECTED -- iter-70** (opens a genuinely NEW perception axis -- cross-file relation -- on the ... |
| 74 | **`pla explain` whole-slate audit (make `--goal-id` optional)** -- when `--goal-id` is omitted, `explain` audits EVERY ranked goal's gate decision in one pass [human: one `_render_ ... | CLI/L2 | High | Low-Med | Scout A C1 (iter-71) | **SELECTED -- iter-72** (the queued top net-new pick, now taken. Makes `--goal-id` optional so ... |

| 75 | **`pla providers [--json]`** -- new read-only, LLM-free, zero-input CLI verb cataloging the LLM BACKEND surface: every accepted provider (== `VALID_PROVIDERS`) + its kind (`offline ... | CLI/L0 | Med-High | Very Low | PM scout (iter-65, roadmap exhausted) | **SELECTED -- iter-65** (ships as commit-sequence **factory iter 75**, test module `test_iter75 ... |
| 76 | **`read_lines(path, start, end)` L1 ACT-sandbox tool** -- read-only, returns the **1-based inclusive line range `[start, end]`** of a file (an arbitrary INTERIOR window), completin ... | L1 | Med-High | Low | PM scout (iter-66, roadmap exhausted) | **SELECTED -- iter-66** (ships as commit-sequence **factory iter 76**, test module `test_iter76 ... |

| 77 | **`SyntaxErrorCollector`** (`kind="syntax_error"`) -- the 15th L2 collector and the FIRST code-PARSING collector: `compile(text, str(full), "exec")` PARSE-ONLY on every `*.py` (nev ... | L2 | Med | Low | PM scout (iter-67, roadmap exhausted) | **SHIPPED -- iter-67** (`4726cb9`, commit-seq factory iter 77; table row restored iter-69 -- it ... |

| 78 | **Wire `PLA_SENSITIVE_CATEGORIES` into `Settings.from_env`** -- the last unwired `Settings` field, completing config.py's "everything overridable via `PLA_*`" contract: a comma-sep ... | L0/config | Med-High | Very Low | PM scout (iter-68, roadmap exhausted) | **SHIPPED -- iter-68** (`cd5a0ff`, commit-seq factory iter 78; table row restored iter-69 -- it ... |

| 79 | **Fix two stale `cli.py` docstring counts + drift-guard all three catalog docstrings** -- `build_parser` docstring "eleven subcommands" -> "fourteen" (14 live subcommands) and `_cm ... | Docs/integrity | Med | Very Low | PM scout (iter-69, roadmap exhausted) | **SELECTED -- iter-69** (commit-seq factory iter 79; the standing iter-01 INTEGRITY tie-break i ... |
| 80 | **Make `pla watch` survive a failed scan** -- add an optional keyword-only `on_error` hook to `scheduler.run_periodic` (default `None` = unchanged/propagate; when set, isolate `sca ... | L0/scheduler | High | Low | PM scout (iter-70) | **SELECTED -- iter-70** (frontier row; the docs-vs-code count landscape re-verified clean [cli. ... |
| 81 | **Ignore ATX headings inside fenced code blocks in `NotesCollector`** -- track triple-backtick / triple-tilde fence state in `notes.py`'s `_extract_note_signals` so an unindented ` ... | L2/notes | High | Low | PM scout (iter-71) | **SELECTED -- iter-71** (frontier row; docs-vs-code count landscape re-verified DORMANT this it ... |
| 82 | **Clamp `RecentFilesCollector` weight to the documented `[0.0, 1.0]` range** -- `filesystem.py`'s recency weight is `max(0.0, 1.0 - age_days / max(within_days, 1))`, which clamps o ... | L2/quality | Med | Very Low | PM scout (iter-72, coldest-module correctness) | **SELECTED -- iter-72** (frontier exhausted; reproduced the future-mtime weight overflow LIVE b ... |
| 83 | **Recognize `*` and `+` Markdown task-list bullets in `TodoCollector`** -- `todos.py`'s `_CHECKBOX_RE` only matches a DASH bullet (`^\s*-\s+\[\s\]\s+`), so GitHub-Flavored-Markdown ... | L2 | Med | Very Low | PM scout (iter-73, coldest-module correctness) | **SELECTED -- iter-73** (frontier exhausted; the git_activity dup-subject drop was REJECTED as ... |
| 84 | **Scan git repos in deterministic name order in `GitActivityCollector`** -- `git_activity.py`'s `_collect` enumerates direct child repos via `root.iterdir()`, whose order Python do ... | L2/quality | Med | Very Low | PM scout (iter-74, coldest-module determinism) | **SELECTED -- iter-74** (frontier exhausted; reproduced the arbitrary iterdir ordering LIVE bef ... |
| 85 | **Scan child git repos in deterministic name order in `WorkingTreeCollector`** -- `working_tree.py`'s `_dirs_to_scan` enumerates direct child repos via `for child in root.iterdir() ... | L2/quality | Med | Very Low | PM scout (iter-75, coldest-module determinism) | **SELECTED -- iter-75** (frontier exhausted; reproduced the arbitrary iterdir summary-signal or ... |
| 86 | **Deterministic ascending-`id` tie-break in `GoalSlate.ranked()`** -- `models.py`'s `ranked()` sorts goals by `(appropriate_now desc, score desc)` with NO tie-break, so two appropr ... | Model/L2 | Med | Very Low | PM scout (iter-76) | **SELECTED -- iter-76** |
| 87 | **Weight-aware per-kind cap in the synthesizer context brief** -- `GoalSynthesizer._build_prompt` (synthesizer.py) caps each kind to the first `_MAX_SIGNALS_PER_KIND` (8) signals v ... | L2/synthesis | High | Low | PM scout (iter-77) | **SELECTED -- iter-77** |
| 88 | **`pla signals --min-weight FLOAT` relevance-threshold view filter on the read-only signals inspector** -- the signals inspector could narrow by KIND (`--kind`) but never by RELEVA ... | CLI/DX | Med | Very Low | PM scout (iter-78) | **SELECTED -- iter-78** |
| 89 | **Deterministic ascending-path tie-break in `RecentFilesCollector` recency sort** -- `filesystem.py` `_collect` sorts candidate files by `key=lambda t: t[0]` (mtime) ALONE then cap ... | L2/quality | Med | Very Low | PM scout (iter-79, coldest-sort determinism) | **SELECTED -- iter-79** |
| 90 | **Deterministic collect-all -> sort-by-`(relpath, lineno)` -> cap in `TodoCollector._collect`** -- `todos.py` `_collect` walked in arbitrary `os.walk` order and `return`ed EARLY fr ... | L2/quality | Med | Very Low | PM scout (iter-80, coldest-collector determinism) | **SELECTED -- iter-80** (frontier exhausted; reproduced the non-deterministic cap selection LIV ... |
| 91 | **Reject non-finite `pla signals --min-weight` (`nan`/`inf`/`-inf`) at parse time** -- `--min-weight` was the ONE numeric CLI arg still declared with a bare `type=float`, so `float ... | CLI/hardening | Med | Very Low | PM scout (iter-81) | **SELECTED -- iter-81** (collector-determinism arc iters 84-90 exhausted for real triggers; rep ... |
| 92 | **Deterministic notes-directory scan order in `NotesCollector`** -- `notes.py`'s `_collect` gathers matching notes-style directories (`notes`/`journal`/`docs`) via `os.walk(root)`, ... | L2/quality | Med | Very Low | PM scout (iter-82, missed-determinism instance) | **SELECTED -- iter-82** (frontier exhausted; reproduced the non-deterministic cap selection + e ... |
| 93 | **Require a genuine JSON boolean `done` to complete an L1 run in `GoalLoop._parse_check`** -- `_parse_check` used `bool(data.get("done", False))`, which coerces ANY truthy non-bool ... | L1/executor | High | Very Low | PM scout (iter-83, coldest-module correctness) | **SELECTED -- iter-83** (frontier exhausted -- all rows 1-92 SHIPPED/SELECTED, so a fresh PM sc ... |
| 94 | **Add `--collector NAME` upstream allowlist to `pla signals`** -- extend the shipped `scan --collector` knob (row #71) to the perception INSPECTOR `pla signals`, giving the first u ... | CLI/DX | Med | Very Low | PM-lead (iter-85, re-pick of the reviewer-APPROVED iter-84 feature blocked by a stale-IPC tester infra hiccup, not a defect) | **SELECTED -- iter-85** (re-pick: iter-84 fully spec'd + engineered + reviewer-APPROVED this EX ... |
| 95 | **Type-clean the `ollama` provider boundary + add mypy config (bite 1 of the "fully type-hinted" oracle)** -- the README's public claim "fully type-hinted (ships a PEP 561 py.typed ... | Integrity/types | Med | Low | OPERATOR-FLAGGED next high-value pick (learnings digest) | **SHIPPED -- iter-86** (`d710b91`; no hotfix/speed flag, no scout slates -> straight PM pick; t ... |
| 96 | **Structural fix of the 2 `loop/tools.py` sandbox-guard mypy errors -> whole `src/proactive_loop` package type-clean (bite 2 of the "fully type-hinted" oracle)** -- after bite 1 (i ... | L1/types/security | Med | Low | OPERATOR-FLAGGED next high-value pick (learnings digest) | **SHIPPED -- iter-87** (`c89ed1d`; no hotfix/speed flag, no scout slates -> straight PM pick; c ... |
| 97 | **Wire mypy into `make typecheck` + a CI step, pinned as a LOCKED dev-dep -> permanent oracle for the "fully type-hinted" README claim (bite 3, FINAL, of the type-hint oracle)** -- ... | Integrity/build | Med | Low | OPERATOR-FLAGGED next high-value pick (learnings digest) | **SHIPPED -- iter-88** (`6303cc6`; no hotfix/speed flag, no scout slates -> straight PM pick; C ... |
| 99 | **`pla signals --summary` per-kind count rollup on the signals inspector** -- add a `--summary` flag to `pla signals` that prints an aggregate kind->count table (+ total) instead of the per-signal listing; with `--json` emits a `{workspace_root, summary, total}` object; composes (AND) with `--kind`/`--min-weight`/`--collector`. First AGGREGATE view (all prior knobs are FILTERS); zero perception/registry change, no count cascade. | CLI/DX | Med | Very Low | PM-lead (iter-92, triaged A2 over A1-collector to avoid the 3-frozen-test count cascade) | **SELECTED -- iter-92** |
| 98 | **`pla runs --status STATUS` filter on the run inspector** -- add a `--status` argument to `pla runs` restricting the listing to runs whose persisted status equals STATUS, `choices ... | CLI/DX | Med | Very Low | PM-lead (iter-89, straight pick) | **SELECTED -- iter-89** (no hotfix/speed flag, no scout slates -> straight PM pick; the run ins ... |

| 100 | **`make setup` uses `uv sync --locked` for local/CI dependency-reproducibility parity** -- CI installs with `--locked` (fails the build on any uv.lock drift) but `make setup` ran bare `uv sync` (silently mutates the lock, hands a contributor an env unlike the graded one); align the recipe + pin it with a behavior test. Zero dependency/CLI-output/count-cascade change; breaks the recent CLI-knob shape by returning to the build/CI-maturity arc (iters 95-97 type oracle, CI landing). | DX/CI | Med | Very Low | PM-lead (iter-93, triaged scout-B B1 over scout-A A3 `explain --summary` to break the CLI-knob shape) | **SHIPPED -- iter-93** (`3e635f8`) |

| 101 | **`LicenseCollector` (`kind="license"`)** -- new content-blind, root-anchored, source-gated L2 collector flagging the open-source-hygiene gap "workspace has source code but no LICENSE file" (case-insensitive basename match of LICENSE/LICENCE/COPYING/UNLICENSE; gap-only, mirrors ci_config's source gate; dogfood-safe -- this repo HAS a LICENSE). Count cascade (all drift-guarded): registry `__init__` + `cli._COLLECTOR_CATALOG` + README "15"->"16" collectors + `test_iter57_helpers` len 15->16. | L2 | High | Low-Med | PM-lead (iter-94, triaged scout-A A1 over scout-B B2 py.typed-wheel test on the tester-cap hazard; a new perception axis also breaks the CLI-knob/DX shape of the last two ships) | **SELECTED -- iter-94** |

| 102 | **`make check` aggregate target + drift-guard** -- add a `.PHONY` `make check` that reproduces the EXACT CI graded gate locally in CI's own order (`uv sync --locked` -> `uv run pytest` -> `uv run mypy src/proactive_loop` -> `$(MAKE) demo` -> the two demo-artifact assertions), completing the CI-parity arc iter-100 opened (`make setup --locked`); one command == a green CI. A pure-text drift-guard test binds the recipe to `.github/workflows/ci.yml` (a red test == recipe/CI drift). Additive Makefile only: zero dependency/`uv.lock`/`src`/count-cascade change, no carved-out README number touched, cap-safe (test does no subprocess/build/network). | DX/CI | Med | Very Low | PM-lead (iter-95, triaged scout-B B1 over scout-A A1 `copy_file` on the tool-count cascade + tester-cap hazard) | **SELECTED -- iter-95** |

| 103 | **Offline-first source-import guard** -- a pure-`ast` behavior test (`tests/test_iter103_behavior.py`) that fails the build if any file under `src/proactive_loop/` imports a network module (socket/ssl/urllib/http/requests/httpx/aiohttp/urllib3), turning the "Offline-first: NO network at runtime or in tests" quality bar into a CI-graded oracle -- the same unbound-claim close the mypy oracle gave "fully type-hinted". Passes today (src has zero network imports; `subprocess` whitelisted for the git collectors; the lazy `botocore` import in `providers.py` is deliberately OUT of the 8-module set). Zero src/dep/`uv.lock`/Makefile/CI/README-number change; the guard is a stdlib-`ast` scan of the src tree AS DATA. | Integrity/DX | High | Very Low | PM-lead (iter-96, triaged scout-B B1 offline guard over scout-A A1 `copy_file` on the tool-count cascade + tester-cap hazard) | **SELECTED -- iter-96** |

_Roadmap owned by the PM-lead role; updated each iteration (mark shipped, re-order on learnings)._
