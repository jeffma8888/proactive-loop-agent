# Enhancement Roadmap

A maturity/enhancement backlog layered on the shipped **v0.1.1**, grounded in
[`SPEC.md`](SPEC.md). The product is already complete and tested (134 tests
green through iter-02); everything here is a small, self-contained increment
that raises the bar of an existing layer — L2 scout perception, L1 loop
execution, L0 resilience/observability, or CLI/DX — without breaking the public
contracts. Each iteration of the dev loop selects exactly one row.

Ordering is by leverage on the product's thesis ("a proactivity layer that is
resilient by design"), balanced against blast radius, with one standing
tie-break: an integrity fix (a live docs-vs-code contradiction on a public
repo) outranks a net-new capability. Per-row reasoning lives in that
iteration's `pm.md`; the full verbatim history is in
[`ROADMAP_ARCHIVE.md`](ROADMAP_ARCHIVE.md).

> **This file is an INDEX, deliberately kept small.** It grew to 200KB of settled history, which pushed the PM stage past the agent CLI's hard 600-second timeout and stalled the loop entirely (iteration 90 lost all four attempts, producing nothing).
>
> Read [`ROADMAP_ARCHIVE.md`](ROADMAP_ARCHIVE.md) ONLY when you need one specific row's detail; never read it wholesale.
>
> **Maintainers (including automated contributors): keep rows one line and terse.** Put a new row's full reasoning in your iteration's `pm.md`, not here.

_Rows are dropped from this index once shipped, and only after verifying the row's full verbatim text is already in `ROADMAP_ARCHIVE.md`: **anything still listed below exists ONLY here, so never drop one without confirming the archive has it.** Shipping and retiring in the SAME commit is ENFORCED, not a convention -- see `settled_rows_needing_retirement`. Shipped rows keep a one-line Done-ledger record below._

| # | Enhancement | Layer | Value | Risk | Source | Status |
|---|-------------|-------|-------|------|--------|--------|
| 109 | **Char-size budget test for the per-iteration required-reading docs** -- `ROADMAP.md` (~40KB operator trigger) and `SPEC.md` (90,269 chars) have NO size guard, and the 200KB-roadmap stall cost iters 90-91 all 8 attempts and 0 commits. Must prove itself against a synthetic over-budget file, and must NOT rewrite `SPEC.md` (operator-owned). | DX/integrity | Med | Very Low | PM scout-B B3 (iter-100) | **QUEUED -- `SPEC.md` HALF ONLY** (the `ROADMAP.md` half shipped as row #138, iter 158). `SPEC.md` is operator-owned at ~90,879 chars with NO operator-documented threshold, so any budget must sit ABOVE today's size -- an arbitrary, ratchet-shaped number. Settle the threshold before coding. |
| 121 | **Close the deferred `disallow_any_generics` flag: the 35 bare-generic (`type-arg`) sites** -- per-file split `cli.py` 19, `loop/tools.py` 15, `loop/executor.py` 1; mechanical `dict` -> `dict[str, Any]`. | Typing/hygiene | Med | Low-Med | PM-lead (iter-108, deferred half of row #120) | **SHIPPED -- iter-139** (factory iter 146). The key was DELETED rather than set `true`, so `strict = true` now stands UNQUALIFIED at zero errors under both `mypy src/proactive_loop` and `mypy --strict`; all 35 sites parameterized with NO value type tightened, no new `type: ignore` and no new per-module override. Full rationale lives in iter-139's `pm.md`; the verbatim pre-ship row text is preserved in [`ROADMAP_ARCHIVE.md`](ROADMAP_ARCHIVE.md). This row is deliberately KEPT in the index rather than dropped on ship, because `test_iter115_behavior.py::test_b8_roadmap_row_owns_the_deferred_flag` requires exactly one row #121 here naming both the flag and `type-arg`. |
| 122 | **`PythonVersionDriftCollector` (`kind="python_version_drift"`)** -- would be the 17th collector and only the 2nd RELATIONAL one (`lockfile_drift` is the sole one today): `requires-python`, an optional `.python-version`, and the CI matrix floor are three independently-edited declarations of ONE fact. `tomllib` is stdlib, so NO new dep and `uv.lock` is untouched. CAVEAT measured iter-108: parsing an Actions matrix without PyYAML is a heuristic line scan, so it must emit ONLY when two floors both parse unambiguously AND disagree -- a false drift signal on a stranger's repo is worse than silence. Silent on this repo (`>=3.12` vs matrix floor 3.12, no `.python-version`), so `make demo` is unaffected. | L2 | Med-High | Low-Med | PM scout-A A2 (iter-108) | **QUEUED** |
| 142 | **SPEC QUESTION FIRST: may `pla signals` write a cross-process cache?** -- row #130's memo dies at process exit, but the dominant invocation is a FRESH process (`pla signals`, `make demo`, every CI step, tick 1 of every `watch`), which measured iter-118 pays 179.7-209.9 ms of a 341-386 ms scan versus 21.0-25.1 ms warm; a persisted digest->verdict map would cut a one-shot scan ~45%. BLOCKED, not queued: `SPEC.md` specifies `pla signals` as "read-only, LLM-free" and `state_dir` appears ZERO times under `collectors/`, so this needs an operator spec amendment plus L2-boundary plumbing, a cache-version key, a size cap and corruption tolerance. Decide the clause before writing code. | L2/throughput | High | Med-High | PM scout-A A3 (iter-118) | **BLOCKED -- needs an operator SPEC decision** |
| 158 | **A re-rooted `--workspace SUB` INVENTS repo-posture findings and leaves the git kinds unscoped** -- measured iter-132 by scout A: under `--workspace src`, `ci_config` emits "no CI configured" and `license` emits "no license file" although `.github/workflows/ci.yml` and `LICENSE` both exist at the root (`license` does not appear in the root scan at all, so re-rooting INVENTS a kind), while `git_commit` stays at full-repo 15 because git discovers the enclosing repo and only its path degrades to the literal workspace string. These are exactly the kinds a user would arm in `--fail-on-kind`. Distinct from #157, which is path SPELLING, not perception SCOPE -- do not bundle them. | L2 | Med | Med | PM scout-A PM-feedback (iter-132) | **QUEUED** |
| 161 | **Give `--baseline` its first consumer: commit a snapshot and arm `ci_config` only** -- the ratchet shipped as row #150 and nothing consults it; the Makefile's "red on arrival" reasoning for the 3 unarmed kinds is now stale. Measured iter-133: of those three only `ci_config` is safely armable (stable identity, and the no-CI case emits a DIFFERENT summary, so a baselined entry becomes a real "the workflow was deleted or renamed" detector); `test_posture` churns BY CONSTRUCTION (its count is inside `summary`, one of the 6 identity keys) so arming it is a self-inflicted public red build; `lockfile_drift` is mtime-driven, baseline-safe but worthless as enforcement. Needs a stated refresh rule or the committed snapshot rots into a blindfold. | L2/enforcement | Med | Med | PM scout-B B2 (iter-133) | **QUEUED** |
| 163 | **Four collectors hand-maintain the root+direct-children walk in TWO divergent flavors, and 2 docstrings assert a parity the code contradicts** -- permissive (unsorted `iterdir()`, every child dir): `git_stash`, `git_state`; strict (`sorted()` + `(child / ".git").exists()` + dedupe): `git_activity`, `working_tree`. `git_stash`'s "identical strategy to the other git collectors" and `git_state`'s "identical strategy to GitActivity/WorkingTree" are BOTH false. Merging the flavors CHANGES the directory set for two collectors, so the only behavior-preserving slice is a parameterized hoist where each caller keeps its own predicate -- do not frame this as a flat deletion. Verified iter-134: the shared body reads no module constant, and both permissive collectors sort by summary before `max_items`, so the unsorted walk is NOT a determinism bug. | Hygiene/DX | Low-Med | Med | PM scout-B B2 (iter-134) | **QUEUED -- HOIST HALF ONLY** (iter-148): docs half carved out to #180 and shipped; remaining slice is the hoist where each caller keeps its own predicate, never a flat deletion. Only THREE `_dirs_to_scan` helpers exist -- `git_activity` walks inline -- so a 4-function hoist is wrong. RE-MEASURED iter-152 by scout B, so the diff is now PRICED: the two permissive bodies are the ONLY exact structural duplicate in `src/` (1.000 over 279 normalised function bodies), and `test_iter154_behavior.py`'s `_body_source` STRIPS docstrings, so the flavor difference must live in the DELEGATE BODY -- passing `gated=False`/`gated=True` or a predicate arg PASSES `test_b04`, while moving it into a class attribute or into prose FAILS. `test_b03` counts `_dirs_to_scan` by NAME, so the shared walker must be named something else (`_child_dirs` adds 0 to the census; reusing `_dirs_to_scan` makes it 4 and fails the build) |
| 165 | **Four MORE collectors build a workspace-relative path inline, in divergent idioms that row #162's guard cannot see** -- measured iter-134: `notes.py` and `todos.py` use `str(file_path.relative_to(root))` with NO `.as_posix()` (backslashes off-POSIX) and no `ValueError` fallback, while `filesystem.py` and `test_posture.py` also omit the fallback, so an outside-root path RAISES and is swallowed to `[]` by the fail-open wrapper -- a whole collector silently empties. Row #162 single-sources only the 6 that had a named helper, by design. CHECK FIRST whether iter-139's CLI-boundary normalization (`_relative_signal_path` in `cli.py`) already makes the posix half moot; if it does, the residual value is the missing fallback only. | Hygiene/DX | Low-Med | Low | PM-lead (iter-134) | **QUEUED -- re-scoped, the scary half is void** (iter-147): all four cited sites DO have the `ValueError` fallback and `git log -S"except ValueError"` returns one commit, the initial implementation, so nothing drifted and the iter-134 measurement was wrong. Residual: `notes.py` / `todos.py` use `str(...)` not `.as_posix()`; `test_posture.py` and `filesystem.py` are defensibly different, not duplicates. |
| 169 | **`addopts` gains `--dist worksteal`** -- warm suite 30.01s -> 26.05s (-13.2%) measured iter-141; one string in `pyproject.toml`, no dep, so `uv.lock` is untouched. TRAPS: `test_iter52_behavior.py` pins `addopts` EXACTLY, and the win is ONE sample on a box that measured 49.98s and 30.01s for the same command minutes apart -- needs >=3 alternating repeats before it is credible. MEASURED STALE iter-166: the warm suite is 60.03s today, so the 30.01s baseline this -13.2% is priced against no longer exists -- re-measure before coding. | DX/throughput | Med | Low-Med | PM scout-B B1 (iter-141) | **QUEUED** |
| 170 | **Prune the armed self-scan gate to the 4 collectors it can fail on** -- `--fail-on-kind` is report-side only (`cli.py`), so 17 collectors run for a verdict only 4 can reach; `--collector` pruning cuts collect 421.6 -> ~285 ms (-29%) at 3 call sites. MUST derive the collector list FROM the armed kinds and pin the two sets equal, or a 5th armed kind goes FAIL-OPEN. | L2/throughput | Low-Med | Med | PM scout-B B2 (iter-141) | **QUEUED -- conditional on a derived-list guard; PRICED iter-147 and still not worth an iteration**: pruning saves 164.41 ms of a 423.84 ms collect (28.2% of the 582.9 ms wall), i.e. 0.16 s absolute, bought with 4 more flags on a gate command that two tests pin byte-for-byte across `make check` and `ci.yml`, plus fail-open risk if a 5th kind is armed without updating the list. The measurement stands; so does the price. |
| 171 | **The 120s BROKEN cliff is steered by a WARM number that reads ~20s low** -- measured iter-141: first-run-in-a-tree 49.98s vs 30.01s warm (1.66x), and post-release runs in a FRESH CLONE while every figure the repo cites about itself is warm. Slice = a `make test-cold` recipe plus correcting the 2-3 warm citations. An INSTRUMENT, not a speed-up; the delta is not yet attributed to bytecode. MEASURED WORSE iter-166: warm is now 60.03s, so the same 1.66x cold ratio projects ~100s against the 120s cliff -- the gap this instrument aims at has doubled. | DX/integrity | Med | Very Low | PM scout-B B3 (iter-141) | **QUEUED** |
| 172 | **Nothing detects a roadmap row that misreports its own ship** -- no `roadmap_ledger_gaps` brake exists here (the sibling `_platform` has one), so SIX rows (#156, #159, #160, #162, #166, #168) read the PRE-ship `SELECTED` while their commits were already in `git log`, none with a Done-ledger line. Repaired by hand in iter-141; the guard is what stops the recurrence. | PM maintenance/integrity | Med | Low | PM-lead (iter-141) | **QUEUED** |
| 173 | **README's intro names 4 of the 6 live providers** (omits `groq` and `together`, both fully branched and stub-tested). BLOCKED, not queued: that line sits ABOVE the `PORTFOLIO INTRO` marker, where the operator carve-out permits exactly three NUMBERS to change and a provider list is not one -- so no iteration may fix it. Recorded so a public docs-vs-code understatement is not invisible. | Docs/integrity | Low-Med | n/a | PM-lead (iter-141) | **BLOCKED -- human-owned README region** |
| 179 | **`SPEC.md` is on the same growth curve that stalled the PM stage once already** -- 90,879 chars measured iter-146 (it was 89,249 when the operator flagged it as the watch item, so it grew rather than stabilised), the largest per-iteration required read, and it is what `cfg.vision` resolves to. The `ROADMAP.md` precedent is measured, not theoretical: at 200,022 chars it pushed the PM stage past the hard 600s cap and iterations 90/91 burned every attempt and shipped nothing. Slice = move the oldest settled module-contract prose to a `SPEC_ARCHIVE.md`, one terse line per contract, plus a char-budget guard. TRAPS: 7 test modules read `SPEC.md` as a file, so the move must be byte-preserving and those tests run FIRST; and shrinking the roadmap was necessary but NOT sufficient, so claim the payoff modestly. | PM maintenance/throughput | Med | Med-High (touches the fixed vision) | PM scout-B B3 (iter-146) | **QUEUED -- act at ~100,000 chars** |
| 182 | **The collector perception depth is not reachable by a user at all (`signals --max-items N`)** -- measured iter-150 by scout A: `todo 30` / `recent_file 20` / `git_commit 15` sit EXACTLY on their collector caps, so 3 of 8 live kinds are silently truncated, and at the cap the shipped `--baseline` ratchet is position-dependent BOTH ways (a 31st TODO sorting last is absent from `--json`; one sorting first EVICTS the tail item). BLOCKING DESIGN QUESTION: the cap lives under FOUR field names (`max_items` x14, `max_files`, `max_commits`, `max_manifests`) and `max_read_bytes` must NOT be touched, so a uniform flag mapping only `max_items` is a LIE for 2 of the 3 saturated kinds. Scope to `signals` only -- raising it for `scan` changes LLM prompt size. Its visibility half (a `capped` field on `--json`/`--summary`) touches a pinned stdout contract and is separable. | CLI/L2 | Med-High | Med | PM scout-A A1+A2 (iter-150) | **QUEUED -- settle the four-field mapping before coding** |
| 184 | **`signals --fail-over N` has had ZERO consumers since iter 145** -- iter-151: the 3 gate sites invoke only `run` and `signals` of 15 verbs, and neither `--fail-over` nor `--baseline` appears in any -- the "advertised but never demonstrated" condition the self-scan step exists to end. A count budget is also the ONLY instrument that can arm the 3 kinds `--fail-on-kind` cannot (non-zero here: red on arrival). TRAPS: `lockfile_drift` is an mtime compare, so re-measure in a FRESH CLONE before pinning N; a 4th gate line joins 3 sites pinned byte-identical by `test_iter102/110/147`. | CI/DX | Med-High | Med | PM scout-B B1 (iter-151) | **QUEUED** |
| 190 | **The L1 budget is REPORTABLE but not SETTABLE (`--max-iterations`/`--max-llm-calls`)** -- iter-155: 0 hits in `src/`; `config --json` publishes both budgets and `run --json` publishes what a run SPENT, yet setting them needs `PLA_*` env, and `--dry-run` runs ZERO iterations so there is no bounded REAL run. `Settings.from_env(**overrides)` already takes kwargs. SEAM TRAP to settle first: on the 3 loop verbs `pla config` goes subtly wrong; on `globals_` the 4 zero-input catalog verbs inherit a flag their SPEC pins INERT. | CLI/DX | Med | Low | PM scout-B B1 (iter-155) | **QUEUED -- reach over an env knob, not new power; name the seam** |
| 191 | **Per-category autonomy thresholds (`PLA_CATEGORY_MIN_SCORE=career:4.5,project:3.0`)** -- `gate()` uses ONE global `auto_dispatch_min_score`, so the only instruments are raising it for everything or forcing approval ALWAYS via `sensitive_categories`. Mirrors iter-30's `PLA_SENSITIVE_CATEGORIES` parser (REPLACE, fail-safe on empty, all-or-nothing on a bad token); default empty = today byte-for-byte. TRAPS: edits the safety boundary AND the pinned `policy --json` narration (`test_iter22` pins `auto_dispatch_threshold`), and must prove a category map can NEVER auto-dispatch a sensitive category. | L2/autonomy | Med-High | Med-High | PM scout-B B3 (iter-155) | **QUEUED -- highest blast radius of the iter-155 slate** |
| 192 | **`run --max-goals N` -- dispatch the top N AUTO_DISPATCH goals, not only the head** -- `_cmd_run` takes the FIRST auto-approved goal and stops (`_cmd_run` in `cli.py`), so on a slate with 3 auto-approved goals the sole autonomous verb acts on one and the user hand-copies the rest into `pla dispatch`. Default 1 keeps every invocation byte-identical. THREE coupled prerequisites, measured iter-156, settle them BEFORE coding: (a) `SPEC.md` says "auto-dispatch **the top** AUTO_DISPATCH goal" and `--dry-run` prints "**the single** goal it WOULD auto-dispatch", so the vision wording bounds this verb and only an operator may widen it; (b) the one-JSON-object `run --json` contract shipped as #186 does not survive N>1 -- decide array vs a `runs: []` wrapper; (c) `examples/scripted_responses.json` holds exactly 8 entries and `ScriptedLLMClient` matches by tag IN ORDER, so a 2nd goal's `plan` call raises `ScriptExhaustedError` -- the bundled driver HARD-FAILS at N=2 and needs new entries in the same commit (the prerequisite shape #152 already paid for once). Distinct from #190, which bounds one run's DEPTH; this bounds a scan's BREADTH | L1/autonomy | Med-High | Med | PM scout-A A1 (iter-156) | **QUEUED -- DISPATCH half only; still blocked on the SPEC wording in (a).** The ACCOUNTING half shipped as #239 (iter 234): `run` now REPORTS the auto-approved goals it skipped, with paste-ready commands, which widens no SPEC wording, needs no 2nd scripted `plan` entry and adds no `run --json` key -- so (a)/(b)/(c) are all still unpaid. |
| 193 | **Ground each goal's `sources` against the signals actually PERCEIVED** -- `CandidateGoal.sources` (`models.py`) is free text the synthesizer prompt asks the model to fill, and nothing verifies one entry corresponds to a signal a collector emitted, so `explain` (`_cmd_explain` in `cli.py`) repeats the model's word for it and a fabricated path is indistinguishable from a real one. Ground truth IS in hand at scan time (`snapshot.signals`), so resolve each source by normalized summary and normalized path. HARD BOUND measured iter-156, do NOT re-derive: the audit surface cannot carry the verdict -- `SPEC.md` closes `explain --json` at "one object of **exactly the 12 keys**" and `test_iter22_behavior.py` + `test_iter72_behavior.py` both pin that set "no more, no fewer", so a 13th key needs an OPERATOR spec amendment. `GoalSlate` also persists no snapshot (`models.py`), and a re-scan at explain time is WRONG (mtime-driven collectors make an unresolved source mean staleness, not fabrication). Report-side and scan-time only; must be conservative -- report unresolved, NEVER delete or downrank a goal -- and the oracle needs a known-GOOD sample that must not be flagged, not only a planted fabrication | L2/trust | Med-High | Med | PM scout-A A3 (iter-156) | **QUEUED -- REPORT-SIDE ONLY; the `explain --json` half is SPEC-FORBIDDEN** |
| 194 | **`scan --suppress FILE` -- let the user retire a goal that keeps coming back** -- nothing in `src/` implements suppression, snoozing or dismissal, so every scan re-synthesizes from scratch and a consciously-declined goal reappears at the same rank forever; `watch`/`diff` can even show it recurring with no way to act. Newline-delimited file (the `--baseline`/`--exclude-path` precedent); identity by normalized TITLE, never `CandidateGoal.id` (`_new_id()` is fresh every scan, so an id-keyed file is dead on arrival). BLOCKING DESIGN QUESTION, settle in this row first: `--top` is a VIEW filter (`cli.py`, "the persisted slate is ALWAYS complete -- the file is the record, stdout is the view") while `--collector` is an INPUT filter, and suppression only earns an iteration if it reaches the gate so `run` cannot auto-dispatch a suppressed goal -- which is the INPUT reading, i.e. the written slate stops recording what was perceived. Also do not spell it `--suppress` bare: this verb already carries TWO suppressions at the SIGNAL layer (`--exclude-path` by LOCATION, `--baseline` by INSTANCE, both in `cli.py`). Needs a refresh rule or it decays into a blindfold, silently (#161's objection); a `suppressed: N` trailer count is the cheap mitigation | L2/UX | High | Med | PM scout-A A2 (iter-156) | **QUEUED -- settle VIEW-vs-INPUT and the flag name before any code** |
| 205 | **7 more test modules sample `git status --porcelain` around a window while sibling xdist workers mutate the same tree** -- iter-166 fixed only `test_iter143`; the named audit list is largely VOID. | test integrity | Low | Low | PM-lead (iter-166) | **QUEUED -- RE-SCOPE OR RETIRE.** Re-measured iter-234 (scout B): 4 of the 5 NAMED modules never touch the real repo (`tmp_path` + monkeypatched `subprocess.run`), and every real window routes through one already-one-way pinned helper. Residual at most 3 unnamed samplers. Evidence: iter-234 `pm_scout_b.md`. |
| 210 | **The other seven `os.walk` collectors still each re-walk the tree, and the `Path.stat` fan-out is a SEPARATE seam** -- iter-171 shipped `collectors/dir_source.py` converting only `dependencies` + `lockfile_drift` (13 traversals -> 12). BATCH 2 iter-183: `secret_file` + `test_posture` (hits 1 -> 3). BATCH 3 iter-237: `merge_conflict` + `broken_link` (hits 3 -> 5, misses 1; **10 physical traversals -> 8** per `cli._collect`; both KEEP `_is_hidden` for FILES). FOUR remain -- `syntax_error` + `large_file` convertible, `notes`/`todos` BLOCKED on #178. Batch-4 traps: rewrite `syntax_error`'s degrade-to-[] monkeypatch on its walk seam (`test_iter77_behavior.py`); `large_file` keeps `_is_hidden`, drops only `_SKIP_DIRS`; LOWER `WALK_BUDGET` (`test_iter192_behavior.py`) to the measured count in the SAME commit, since `test_iter193`'s ratchet has ZERO slack; and in THAT SAME commit correct `dir_source.py`, still pricing this seam at 13 traversals / "roughly a third of the scan" on a PRE-module 3,000-file workspace (6 walks / 3.1% here, iters 232+238) -- state BOTH populations and bind any prose integer to the published `WALK_BUDGET`, never to a live walk (that oracle passes only on this machine and dies in the fresh-clone preship). `filesystem.py` is EXCLUDED by design (recency). Do NOT bundle the `Path.stat` seam. | L2/throughput | Med | Low-Med | PM-lead (iter-171, deferred half of #209) | **QUEUED -- batch 3 of 4 shipped iter-237; convert in small batches, never nine in one commit** |
| 212 | **`run`/`dispatch --allow-tool NAME` -- narrow the ACT sandbox for one dispatch** -- the autonomy contract gates only WHETHER a goal runs; every dispatched run then gets the identical full 14-tool sandbox, `remove_file` and `move_file` included. Measured absent iter-174 (five spellings, 0 hits in `src/`+`tests/`, two-sided against `write_file`'s 156). TWO TRAPS: `resume` is the second `ToolRegistry(` site (`cli.py`) and rebuilds from the checkpoint, so the allowlist MUST persist through `_write_meta`/`_read_meta` in the SAME commit -- a safety flag that silently stops applying is worse than none; and the narrowing must live on the INSTANCE, since `tool_names()` feeds the drift-guarded `pla tools` catalog. Full pricing in iter-174's `pm.md`. | L1/autonomy | High | Med | PM scout-A A3 (iter-174) | **QUEUED -- deferred from iter-174 on a measured `stage-budget: WARN` (engineer 600.0s median, 0.0s headroom): a 2-file coupled edit threading a parameter through `_execute_goal`'s 3 call sites.** |
| 218 | **The suite is not environment-hermetic: one exported `PLA_*` knob reds a clean checkout** -- GENERAL (conftest) half only; the measured 3-module slice shipped as row #219 (iter-181). RE-MEASURED iter-181: `PLA_SENSITIVE_CATEGORIES=career uv run pytest` still exits 1 with **13 failed across 9 modules** #219 never touched (`test_cli_integration`, `test_iter06/12/24/27/34/56/62/163`), while `PLA_AUTO_DISPATCH_MIN_SCORE` is now fully CLOSED (3 failed -> 0) -- so the residual defect is ONE knob, 13 tests, 9 modules, and this row's original "3 failed" was simply the one reproduction its author ran. There is NO `tests/conftest.py`, so nothing strips the namespace `config.py` builds every name from (`ENV_PREFIX = "PLA_"`); 12 modules call `Settings.from_env(` and 99 invoke `main()`, so per-call-site fixes cannot cover it. CI is green only because a runner exports no `PLA_*`, while `README.md` PUBLISHES the trigger as the supported config surface. MEASURED BLOCKER, and why iters 180 AND 181 both took a smaller bite: a `tests/conftest.py` escapes every corpus guard whose domain is `TESTS_DIR.glob("test_*.py")`, and an anchored census now finds **8 such sites across 6 modules** (`test_iter108`, `test_iter149`, `test_iter150`, `test_iter159`, `test_iter177`, plus `test_iter185` added by #219 itself), on top of retiring the explicit pin `test_iter159` `test_b7_no_conftest_was_added`. The honest slice is a coupled commit (new conftest + extend those 8 sites + retire that pin), not "one new file that touches no product code". | L0/suite | High | Med | PM scout-B B1 (iter-180) | **QUEUED -- general (conftest) half, RE-SCOPED iter-181** after row #219 shipped the measured 3-module slice; deferred again on a measured `stage-budget: WARN` (engineer 600.0s median, 0.0s headroom). Price the 8-site + pin edit before starting. |
| 231 | **`SPEC.md` section 2's layout tree names 8 of 15 tracked top-level entries, omitting all three machine gates** -- measured iter-197: the tree lists `pyproject.toml`/`Makefile`/`README.md`/`LICENSE`/`SPEC.md`/`src/`/`examples/`/`tests/` while `git ls-files \| awk -F/ '{print $1}' \| sort -u` returns 15; omitted are `.github/workflows/ci.yml`, `hooks/pre-commit`, `DIRECTIONS.md`, `ROADMAP.md`, `ROADMAP_ARCHIVE.md`, `uv.lock`, `.gitignore`, so a reader concludes the project has no CI, no commit hook and no decision log. The same block also omits `examples/check_run.py` (0 hits in SPEC vs 3 in README) though `make demo` runs it. This is the exact rot `test_spec_layout_contract.py` was written for, one line from where it looked -- it bound the collectors line and the verb count only. TWO TRAPS: scope the guard to TOP-LEVEL DEPTH only (`src/proactive_loop/` legitimately elides 17 of 36 `.py` files because 4.1/4.5 are the authoritative rosters), and `.gitignore` needs an explicit exempt set spelled out in the test docstring, since an exemption is the one place a guard can never look. Fold in the sibling defect at `SPEC.md` (`Makefile # setup / test / demo / clean targets` names 4 of 9 real recipes, hiding `typecheck` and `check-matrix` -- the two that constitute the quality bar). | Docs/integrity | Med-High | Low | PM scout-A A1+A2 (iter-197) | **QUEUED -- deferred from iter-197 to break a 3-in-9-commits run of prose-roster guards (iters 189/193/198), not on doubt about the finding** |
| 232 | **`test_catalog_has_sixteen_entries` asserts 17** -- measured iter-197: `tests/test_iter57_helpers.py` carries the count in its FUNCTION NAME while its body asserts `len(_COLLECTOR_CATALOG) == 17` and `len(all_collectors()) == 17`, so the name is stale against its own assertion and misreports the collector roster to anyone grepping test names. Rename only; the assertions are correct. Same class as the spelled-out-count ripple priced in iter-197's `pm.md`. | test integrity | Low | Very Low | PM-lead (iter-197) | **QUEUED** |
| 257 | **Cap the xdist pool -- keep `-n auto`, add `--maxprocesses=N`** -- measured iter-256 by scout A: -26.1% suite wall clock (64.98s -> 48.03s at `-n 8`), 12.4x contention on the floor-setting nested-child test, whole suite still green. BLOCKED behind coordinated edits to three shipped exact-equality `addopts` pins: `tests/test_iter52_behavior.py` (`test_eb3_pytest_addopts_is_pinned_and_coverage_free`), `tests/test_iter142_behavior.py` (an exact string, a token list, AND the cross-module literal assertion `test_eb11_iter52_expected_addopts_constant_agrees`) and `tests/test_iter159_behavior.py` (`test_b7_addopts_is_still_exactly_q_n_auto`). Inert on the 2-core CI matrix, where `-n auto` already yields 2. | DX/throughput | Med | Med | PM scout-A A1 (iter-256) | **QUEUED -- blast radius MEASURED; price the three-oracle edit before starting** |

## Done ledger (one terse line per row recorded since iter-132 -- NOT every shipped row)

A line records that a row SHIPPED, not that the archive holds its text (that happens on retirement); keep rows terse. **A row's cited iteration tag is the tag that appears VERBATIM in the shipping commit's subject** -- spell it `(foundry iter N)` exactly as `git log --format=%s` does, never a second private counter. `tests/test_iter218_behavior.py` reds a cited tag that resolves to no commit subject; the newest row alone is exempt. Oldest bullets RELOCATE into `ROADMAP_ARCHIVE.md` as this file tightens, and `tests/test_roadmap_ledger_conservation.py` reds a row recorded in neither document or in both.

- #168 signals --fail-over N: a count-budget exit gate (iter 138, factory iter 145, 004f037)
- #215 `SPEC.md` §4.1/§4.5 gain an oracle bound to the live registries + 4 omissions (iter 178, factory iter 182)
- #219 3 default-asserting modules clear a derived `PLA_*` set, dead name dropped (iter 181, factory iter 185)
- #185 The demo persists its snapshot and both gates verify the slate's cited sources resolve (iter 182, factory iter 186)
- #220 secret_file + test_posture read the shared dirent listing; 4 collectors share 1 walk (iter 183, factory iter 187)
- #221 CODE5_PRODUCERS derived from cli.py: 3 exit-5 gates guarded on 3 surfaces (iter 184, factory iter 188)
- #223 README links its 4 tracked companion docs, guarded by a git-derived domain (iter 185, factory iter 189)
- #224 scan --json aliases --format json via a store_const in an exclusive group (iters 187-188, factory iter 190)
- #225 Per-scan work-budget oracle: traversals, parses and child processes as ceilings (iter 189, factory iter 191)
- #178 notes + todos import the one walk-prune policy; 1 definition site, AST-censused (iter 190, factory iter 192)
- #226 SPEC.md section 2: guarded counts replace 4-of-17 collector + 14-of-16 verb rosters (iter 191, factory iter 193)
- #136 Both _out_dir_guard rejections pinned verbatim, plus the accept path and precedence (iter 192, factory iter 194)
- #227 make demo publishes run.json and grades it with the committed examples/check_run.py (iter 193, factory iter 195)
- #228 check_run.py reads the persisted checkpoint back and grades run_id/status (iter 194, factory iter 196)
- #229 Hoist the duplicated _log_absorbed onto BaseCollector, plus its first oracle (iter 195, factory iter 197)
- #230 base.py's two stale 16-collector counts corrected to 17, bound to all_collectors() (iter 196, factory iter 198)
- #233 dispatch --dry-run: preview one approved goal's dispatch before any client or run dir (iter 198, factory iter 200)
- #222 README exit-code preamble names `pla --help`, the one surface `-OO` cannot delete (iter 227, factory iter 201)
- #151 `_read_meta` names the malformed `meta.json`, so `resume`'s exit-1 line identifies the file (iter 228, factory iter 202)
- #234 broken_link masks the DESTINATION, not the label, so a backticked-label dead link is seen (iter 229, factory iter 203)
- #235 make demo publishes the autonomy audit and examples/check_autonomy.py grades it (iter 230, factory iter 204)
- #236 Budget the two seams #225 left open: Path.stat fan-out and read_text decodes (foundry iter 231)
- #237 cli.py module docstring names the live verb count; partial 15-of-17 roster dropped (foundry iter 232)
- #238 scan --exclude-path: the perception INPUT filter reaches the synthesis verb (foundry iter 233)
- #239 run reports the AUTO_DISPATCH goals it skipped, with paste-ready commands (foundry iter 234)
- #240 Relocate the settled Done-ledger tail to the archive under a conservation guard (foundry iter 235)
- #241 Paste-ready dispatch commands carry an absolute --slate path (foundry iter 236)
- #242 merge_conflict + broken_link read the shared dirent listing; 6 collectors share 1 walk (foundry iter 237)
- #243 todos gates its checkbox pass on the \[\s\] subpattern _CHECKBOX_RE requires (foundry iter 238)
- #244 Done-ledger iteration tags relabelled to the vocabulary git carries, under a resolvability oracle (foundry iter 239)
- #245 pla policy --check-goal: gate a user-named goal with no slate, no LLM, no workspace (foundry iter 240)
- #117 Packaging-contract oracle: the declared `[project.scripts]` string resolves by import to `cli.main` (foundry iter 241)
- #246 run --json publishes the AUTO_DISPATCH goals it deferred, as an always-present seventh key (foundry iter 242)
- #247 The live index cites evidence by path and symbol: every line-number anchor retired under a two-sided ban (foundry iter 244)
- #248 run --collector NAME: the sole autonomous verb's perception becomes scopeable, and it reports the narrowing (foundry iter 245)
- #249 run --exclude-path GLOB: the sole autonomous verb's perception narrows by LOCATION too (foundry iter 249)
- #250 Spelled subparser counts in cli.py comments+docstrings bound to the live roster; the stale fifteen fixed (foundry iter 250)
- #251 Exit code 1's gate-unproven producer named on the epilog + docstring, under a derived guard (foundry iter 251)
- #252 run --baseline FILE: the sole autonomous verb suppresses by INSTANCE what a saved snapshot already recorded (foundry iter 252)
- #253 README documents make setup + make clean; the target matcher tightens to the make-invocation form (foundry iter 253)
- #254 Both graded gates budget the signal count with pla signals --fail-over (foundry iter 254)
- #255 One atomic_write_text primitive replaces the temp-sibling + os.replace idiom hand-copied at four writers (foundry iter 255)
- #256 SPEC.md section 4.5 documents the 9 shipped CLI long flags it omitted, under a parser-derived census oracle (foundry iter 256)
- #257 run refuses an aliased --baseline/--snapshot path pair as a usage error (foundry iter 257)
- #258 The aliased --baseline/--snapshot guard degrades to absolute() when the OS refuses to resolve, so an unresolvable path refuses cleanly on both CI legs (foundry iter 258)
