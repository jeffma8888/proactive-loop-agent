# Enhancement Roadmap

A maturity/enhancement backlog layered on the shipped **v0.1.1**, grounded in
[`SPEC.md`](SPEC.md). The product is already complete and tested (134 tests
green through iter-02); everything here is a small, self-contained increment
that raises the bar of an existing layer — L2 scout perception, L1 loop
execution, L0 resilience/observability, or CLI/DX — without breaking the public
contracts. Each iteration of the dev loop selects exactly one row.

Ordering is by leverage on the product's thesis ("a proactivity layer that is
resilient by design"), balanced against blast radius, with one standing
tie-break: an integrity fix (a live docs-vs-code contradiction on a public
repo) outranks a net-new capability. Per-row reasoning lives in that
iteration's `pm.md`; the full verbatim history is in
[`ROADMAP_ARCHIVE.md`](ROADMAP_ARCHIVE.md).

> **This file is an INDEX, deliberately kept small.** It grew to 200KB of settled history, which pushed the PM stage past the agent CLI's hard 600-second timeout and stalled the loop entirely (iteration 90 lost all four attempts, producing nothing).
>
> Full rationale for every row -- the complete status prose and the iteration-by-iteration narrative -- is preserved verbatim in [`ROADMAP_ARCHIVE.md`](ROADMAP_ARCHIVE.md). Read that file ONLY when you need one specific row's detail; never read it wholesale.
>
> **Maintainers (including automated contributors): keep rows one line and terse.** Put a new row's full reasoning in your iteration's `pm.md`, not here. If this file passes ~40KB again the loop will stall the same way.

_Settled rows are dropped from this index once shipped -- rows 1-70 in iter-101, rows 71-98 in iter-116, and 28 of rows 99-145 in iter-123 -- after per-row verification that each one is present in [`ROADMAP_ARCHIVE.md`](ROADMAP_ARCHIVE.md), which holds their full verbatim text. **The archive now covers rows 1-98, the 28 settled rows of 99-145 moved in iter-123, and the 11 shipped rows moved in iter-132 (#128, #134, #139, #141, #145, #147, #148, #150, #152, #153, #154 -- each verified ABSENT from the archive before the move and present after), the 5 shipped rows retired in iter-150 (#125, #131, #176, #177, #180 -- each verified ABSENT from the archive before the move and to ALREADY carry a Done-ledger line here, which is the record `test_iter145_behavior.py::test_b12` accepts in place of an index row), and the 10 shipped rows retired in iter-144 (#156, #159, #160, #162, #164, #166, #167, #168, #174, #175 -- each verified to ALREADY carry a Done-ledger line here before the move, and its verbatim index text appended to the archive's bullet section in the same commit), and the 5 rows retired in iter-153 (#105 ABANDONED, #149, #181, #183, #186 -- each verified ABSENT from the archive before the move and present after, and the 4 shipped ones verified to ALREADY carry a Done-ledger line here); anything still listed below exists ONLY here, so never drop a row without first confirming the archive has it.** Shipped rows keep a one-line record in the Done ledger at the foot of this file, so `grep` still answers "did we ship that?" without opening the archive. Look up a single row by number; never read that file wholesale._

| # | Enhancement | Layer | Value | Risk | Source | Status |
|---|-------------|-------|-------|------|--------|--------|
| 109 | **Char-size budget test for the per-iteration required-reading docs** -- `ROADMAP.md` (~40KB operator trigger) and `SPEC.md` (90,269 chars) have NO size guard, and the 200KB-roadmap stall cost iters 90-91 all 8 attempts and 0 commits. Must prove itself against a synthetic over-budget file, and must NOT rewrite `SPEC.md` (operator-owned). | DX/integrity | Med | Very Low | PM scout-B B3 (iter-100) | **QUEUED** |
| 117 | **Packaging-contract oracle** -- `[project.scripts] pla = "proactive_loop.cli:main"` and the `py.typed` marker are asserted by ZERO oracles (CI and every local run use the SOURCE tree), so renaming `main` or dropping the marker from the wheel stays green. Must stay OFFLINE: assert the declared entry point resolves to a real callable + the marker is in the built package file list; a real `pip install` needs network and is FORBIDDEN. Shrinks to entry-point resolution alone if the marker already ships. | Packaging/integrity | Med | Low | PM scout-A A3 (iter-104) | **QUEUED** |
| 118 | **Consolidate the 3 divergent `_SKIP_DIRS`/`_is_hidden` definitions** -- `filesystem.py` has 7 entries, `notes.py`/`todos.py` 6 each (both missing `.tox`); 13 use sites, and `loop/tools.py` (L1 ACT sandbox) imports a private name from an L2 leaf collector. CAVEAT measured iter-104 by scout B: the divergence is behaviorally INERT (all missing entries are hidden names and every site ANDs `not _is_hidden(d)`), so the diff is cosmetic and stands ONLY on an `ast` single-definition drift guard. | Hygiene/DX | Low-Med | Low | PM scout-B B1 (iter-104) | **QUEUED (cosmetic -- guard is the only value)**; re-proposed iter-110 as a user-visible `.tox` leak and FALSIFIED AT RUNTIME by the PM lead: `_is_hidden` is `startswith('.')` and all 3 walk sites AND it, so a live probe (TODO + note planted under `.tox/`) emitted ZERO `.tox` signals -- the proposed behavior test would PASS unchanged, i.e. vacuous. Iter-104's INERT caveat stands. |
| 121 | **Close the deferred `disallow_any_generics` flag: the 35 bare-generic (`type-arg`) sites** -- per-file split `cli.py` 19, `loop/tools.py` 15, `loop/executor.py` 1; mechanical `dict` -> `dict[str, Any]`. | Typing/hygiene | Med | Low-Med | PM-lead (iter-108, deferred half of row #120) | **SHIPPED -- iter-139** (factory iter 146). The key was DELETED rather than set `true`, so `strict = true` now stands UNQUALIFIED at zero errors under both `mypy src/proactive_loop` and `mypy --strict`; all 35 sites parameterized with NO value type tightened, no new `type: ignore` and no new per-module override. Full rationale lives in iter-139's `pm.md`; the verbatim pre-ship row text is preserved in [`ROADMAP_ARCHIVE.md`](ROADMAP_ARCHIVE.md). This row is deliberately KEPT in the index rather than dropped on ship, because `test_iter115_behavior.py::test_b8_roadmap_row_owns_the_deferred_flag` requires exactly one row #121 here naming both the flag and `type-arg`. |
| 122 | **`PythonVersionDriftCollector` (`kind="python_version_drift"`)** -- would be the 17th collector and only the 2nd RELATIONAL one (`lockfile_drift` is the sole one today): `requires-python`, an optional `.python-version`, and the CI matrix floor are three independently-edited declarations of ONE fact. `tomllib` is stdlib, so NO new dep and `uv.lock` is untouched. CAVEAT measured iter-108: parsing an Actions matrix without PyYAML is a heuristic line scan, so it must emit ONLY when two floors both parse unambiguously AND disagree -- a false drift signal on a stranger's repo is worse than silence. Silent on this repo (`>=3.12` vs matrix floor 3.12, no `.python-version`), so `make demo` is unaffected. | L2 | Med-High | Low-Med | PM scout-A A2 (iter-108) | **QUEUED** |
| 129 | **File-level cheap prefilter before the per-line regex loop in `todos` (and `merge_conflict`)** -- measured iter-112 via the shipped `--timings`: a scan is 433.1 ms, of which `todos` 128.8 (30%) and the per-line pass is nearly all of it (52.1 ms of a 70 ms warm `_collect` is `splitlines` + 2 regex calls per line). CAVEAT the engineer must respect: a wrong prefilter SILENTLY DROPS signals in the L2 perception layer, and `[\s]` admits a TAB so a `"[ ]"` substring prefilter is UNSOUND -- the prefilter must be provably weaker than the regexes. Second caveat: durations are barred from every stdout contract here, so the suite can only assert byte-identical output, i.e. it cannot verify the win; whoever ships it must measure it at the gate. | L2/throughput | Med | Low-Med | PM scout-A A1 (iter-112), deferred vs the row #126 integrity fix | **QUEUED** (SHAPE SETTLED by scout-A measurement iter-124, memo bypassed, 190 files/68,906 lines: the sound `text.lower()`+substring prefilter nets **+18.78 ms** (53.99 -> 35.21 ms), a single whole-buffer `re.IGNORECASE` prefilter is a measured net **LOSS of -6.66 ms** (the prefilter alone costs 37.19 ms, 69% of the loop it skips), and a case-SENSITIVE prefilter drops **8 files on this repo**. Residual unsoundness to close in the diff: a `str` pattern's `\s` is UNICODE (29 codepoints), so pin the derived set as a constant and RE-DERIVE it in a test) |
| 136 | **Oracle the 2 unguarded `_out_dir_guard` messages** -- measured iter-116: no test file contains `--out-dir is not a directory` or `--out-dir parent`, and no test names `_out_dir_guard`, so the NEWEST of the 3 CLI path guards (row #132) is the least guarded while `test_iter44_behavior.py` pins the other 3 messages verbatim. | CLI/test | Low-Med | Very Low | PM scout-B B1 (iter-116), separated from its guard-collapse by PM-lead | **QUEUED** |
| 137 | **Put the `watch` -> `diff` change feed inside the graded gate (`make check` + `ci.yml`)** -- the 2 newest features are exercised by NO gate step. PREREQUISITE (measured iter-116): `examples/scripted_responses.json` has ONE `synthesize` of 7 entries and `ScriptedLLMClient` raises `ScriptExhaustedError` in order, so a 2-tick watch exhausts on tick 2 and leaves nothing to diff -- add a 2nd `synthesize` FIRST or it is fail-open gate theatre. `test_iter102_behavior.py` pins `make check` to `ci.yml` (same commit). | CI/DX | Med-High | Med | PM scout-A A2 (iter-116) | **QUEUED -- gate-step half only** (the measured prerequisite ships separately as row #152 in iter-128, factory iter 135, so what remains here is the `make check` + `ci.yml` step itself plus the positional `ARTIFACT_ASSERTION_STEPS` re-bounding. RE-MEASURED iter-134 by scout A: the fail-open caveat is now FALSE -- a 2-tick watch then `diff --dir` yields `+ added (1)` / `- removed (1)` / `~ changed (1)` / `unchanged: 2` in 649 ms total. But the PM-lead re-rated its VALUE DOWN the same run: `uv run pytest` is itself a graded step of BOTH `make check` and `ci.yml`, and inside it `test_iter135_behavior.py` already drives a real 2-tick watch against the bundled driver while `test_iter123_behavior.py` drives `diff --dir/--old/--new/--json`, so both verbs ARE machine-checked on every push -- what this row still buys is a slower second path through the console script, paid for by re-deriving the positional `ARTIFACT_ASSERTION_STEPS` bounds) |
| 138 | **Char-size budget guard for `ROADMAP.md`** -- iter-116 cut rows 71-98 (36,203 -> 28,899 chars, ~20 iterations of headroom), but nothing stops re-growth, and this file has killed the loop once (iters 90-91, 0 commits/9h). RE-MEASURED iter-121 (git show of each sha, piped to wc -c): 28,947 -> 30,672 -> 34,210 -> 35,222 -> 36,381 = **+1,859 chars/iteration**, so iter-116's "~20 iterations" was ~5x optimistic and only ~2 iterations of headroom remain under the operator's 40,000 threshold. Pairs with row #109. TRAP for any future cut: `ROADMAP_ARCHIVE.md` holds rows 1-98 ONLY, so rows 99+ exist solely here -- verify archive coverage per-row before deleting anything. | PM maintenance | Med | Very Low | PM scout-B B3 (iter-116) | **QUEUED** (iter-141: index is back to ~37,000 chars after 5 new rows, so only ~1-2 iterations of headroom remain under the operator's 40,000 threshold -- the NEXT PM should compact settled rows into the archive rather than wait for the trigger. RE-MEASURED iter-123: the index reached 38,074 chars, confirming +1,859/iter, so the PM-lead compacted the 28 settled rows of 99-145 into the archive as maintenance in the same commit -> 17,458 chars, ~11 iterations of headroom; 3rd compaction after iter-101 and iter-116. The GUARD itself is still unbuilt, which is why this keeps recurring. RE-MEASURED iter-150: the index reached **39,617 chars** once this iteration's 2 new rows were added (+2,022/iteration over the 5 commits since the iter-144 compaction to 28,753), i.e. **383 chars short of the operator's 40,000 trigger**, so the PM-lead compacted 5 settled shipped rows into the archive in the SAME commit -- 39,617 -> **33,416 chars**, ~3 iterations of headroom. That is the FIFTH hand compaction (iters 101, 116, 123, 144, 150), which is the whole argument for finally building the guard. #143 and #146 were deliberately KEPT despite being settled: both are do-not-re-derive records whose value is being greppable in the index. #121 must STAY (`test_iter115_behavior.py::test_b8_roadmap_row_owns_the_deferred_flag` requires exactly one)) |
| 142 | **SPEC QUESTION FIRST: may `pla signals` write a cross-process cache?** -- row #130's memo dies at process exit, but the dominant invocation is a FRESH process (`pla signals`, `make demo`, every CI step, tick 1 of every `watch`), which measured iter-118 pays 179.7-209.9 ms of a 341-386 ms scan versus 21.0-25.1 ms warm; a persisted digest->verdict map would cut a one-shot scan ~45%. BLOCKED, not queued: `SPEC.md:846` specifies `pla signals` as "read-only, LLM-free" and `state_dir` appears ZERO times under `collectors/`, so this needs an operator spec amendment plus L2-boundary plumbing, a cache-version key, a size cap and corruption tolerance. Decide the clause before writing code. | L2/throughput | High | Med-High | PM scout-A A3 (iter-118) | **BLOCKED -- needs an operator SPEC decision** |
| 143 | **Measured DEAD, recorded so nobody re-derives them** (iter-118 instrumentation, do not re-propose): `merge_conflict` digest memo nets **+1.09 ms** (whole scan pass is 4.51 ms vs a 3.42 ms digest; its 26.4 ms warm cost is walk+stat+read); `ast.parse` / `PyCF_ONLY_AST` instead of `compile(exec)` is **indistinguishable** (145.93 / 145.51 / 145.95 ms over the same 174-file corpus, error attributes identical); collapsing the 10 full-tree walks into one caps at **~8.6 ms, 2.4%** (the pruned tree is 14 dirs / 190 files); git spawns are only **3 per scan**, root-only. METHOD TRAP: an `os.walk` instrumentation wrapper must be a GENERATOR passthrough, or `dirnames[:]` pruning is defeated and every count inflates ~8x. | L2/throughput | n/a | n/a | PM scout-A (iter-118) | **CLOSED -- measured not worth an iteration** |
| 146 | **Extract the bounded digest-memo mechanism into ONE shared helper** -- iter-123 leaves TWO near-identical module-level memos (`syntax_error._PARSE_MEMO`, `todos`' item memo): each has a cap constant, FIFO eviction, a `clear_*()` and a `*_stats()` seam, ~40 lines apiece. Deliberately NOT bundled into iter-123: the shipped `syntax_error` seam carries 11 assertions bound to its exact names, including a monkeypatch of `PARSE_MEMO_MAX_ENTRIES` proving the cap is read at RUNTIME, so a shared class must late-bind that cap or the oracle breaks. Two instances is the minimum needed to see the shape. | Hygiene/DX | Low-Med | Low-Med | PM-lead (iter-123, deferred half of row #141) | **CLOSED -- premise contradicted by the source itself** (iter-147): `todos.py:66-68` documents the two memos as deliberately separate "so neither module's oracle can be broken by a change made for the other", which IS the risk this row rates highest (11 assertions bound to `syntax_error`'s names, one monkeypatching the cap to prove it is read at runtime). They are also asymmetric -- `todos` carries `TODO_MEMO_MAX_ITEMS_PER_FILE = 256` with no `syntax_error` analogue -- so a shared helper needs an asymmetric guard, not a simplification. Reversing a documented decision requires defeating its stated reason; nobody has. |
| 151 | **Make `resume` tolerant of a corrupt `meta.json` the way the run LISTING already is** -- `_read_meta` (`cli.py:1698-1701`) is a bare `json.loads`, and `_cmd_resume` (`cli.py:3289`) calls it UNWRAPPED while `_run_row` (`cli.py:1744-1747`) wraps the identical call in `except (ValueError, OSError)` and degrades to `{}`, so the SAME corrupt file is tolerated by `runs` and fatal to `resume` (one `error:` line, exit 1) even when the checkpoint beside it is intact. Iter-127 shipped the WRITE side (row #134) and deliberately left this: the open design question is WHICH degrade is right, because silent `{}` inherits `_cmd_resume`'s `Path(".")` fallback and would resume against the CWD as if it were the workspace -- so the honest shape is probably a named error that names the file, or a `--workspace` override on `resume`, NOT copying the listing's silence. README:318 also lists only a malformed slate or `checkpoint.json` under exit 1, never `meta.json`. | L0/durability | Low-Med | Low | PM-lead (iter-127, deferred half of row #134) | **QUEUED** |
| 155 | **Root-Markdown table guard audits the WORKING DIR, not the repo** -- `test_iter133_behavior.py:404` globs `REPO_ROOT` (7 files here) where git tracks 5, so its verdict depends on untracked scratch files; and its anti-vacuous floor names only 3 files, so tracked public `SPEC.md`/`DIRECTIONS.md` can leave the audited set and still pass. Row #147's status names 2 untracked files, so no clone reproduces it. Fix = key on the TRACKED set, assert set EQUALITY. | DX/integrity | Med | Very Low | PM scout-A A1 (iter-131) | **QUEUED** (front-runner; rated strongest alternative to #150 in iter-131) RE-MEASURED iter-151 by scout A: the two extra files (`LINKEDIN_POST.md`, `README_TOP.md`) are not merely untracked, they are excluded via `.git/info/exclude` lines 11-12 -- a PER-CLONE, uncommitted mechanism -- so this guard audits 2 files no other clone and no CI checkout has at all. |
| 158 | **A re-rooted `--workspace SUB` INVENTS repo-posture findings and leaves the git kinds unscoped** -- measured iter-132 by scout A: under `--workspace src`, `ci_config` emits "no CI configured" and `license` emits "no license file" although `.github/workflows/ci.yml` and `LICENSE` both exist at the root (`license` does not appear in the root scan at all, so re-rooting INVENTS a kind), while `git_commit` stays at full-repo 15 because git discovers the enclosing repo and only its path degrades to the literal workspace string. These are exactly the kinds a user would arm in `--fail-on-kind`. Distinct from #157, which is path SPELLING, not perception SCOPE -- do not bundle them. | L2 | Med | Med | PM scout-A PM-feedback (iter-132) | **QUEUED** |
| 161 | **Give `--baseline` its first consumer: commit a snapshot and arm `ci_config` only** -- the ratchet shipped as row #150 and nothing consults it; the Makefile's "red on arrival" reasoning for the 3 unarmed kinds is now stale. Measured iter-133: of those three only `ci_config` is safely armable (stable identity, and the no-CI case emits a DIFFERENT summary, so a baselined entry becomes a real "the workflow was deleted or renamed" detector); `test_posture` churns BY CONSTRUCTION (its count is inside `summary`, one of the 6 identity keys) so arming it is a self-inflicted public red build; `lockfile_drift` is mtime-driven, baseline-safe but worthless as enforcement. Needs a stated refresh rule or the committed snapshot rots into a blindfold. | L2/enforcement | Med | Med | PM scout-B B2 (iter-133) | **QUEUED** |
| 163 | **Four collectors hand-maintain the root+direct-children walk in TWO divergent flavors, and 2 docstrings assert a parity the code contradicts** -- permissive (unsorted `iterdir()`, every child dir): `git_stash:85`, `git_state:80`; strict (`sorted()` + `(child / ".git").exists()` + dedupe): `git_activity:107`, `working_tree:223`. `git_stash`'s "identical strategy to the other git collectors" and `git_state`'s "identical strategy to GitActivity/WorkingTree" are BOTH false. Merging the flavors CHANGES the directory set for two collectors, so the only behavior-preserving slice is a parameterized hoist where each caller keeps its own predicate -- do not frame this as a flat deletion. Verified iter-134: the shared body reads no module constant, and both permissive collectors sort by summary before `max_items`, so the unsorted walk is NOT a determinism bug. | Hygiene/DX | Low-Med | Med | PM scout-B B2 (iter-134) | **QUEUED -- HOIST HALF ONLY** (iter-148): docs half carved out to #180 and shipped; remaining slice is the hoist where each caller keeps its own predicate, never a flat deletion. Only THREE `_dirs_to_scan` helpers exist -- `git_activity` walks inline -- so a 4-function hoist is wrong. RE-MEASURED iter-152 by scout B, so the diff is now PRICED: the two permissive bodies are the ONLY exact structural duplicate in `src/` (1.000 over 279 normalised function bodies), and `test_iter154_behavior.py`'s `_body_source` STRIPS docstrings, so the flavor difference must live in the DELEGATE BODY -- passing `gated=False`/`gated=True` or a predicate arg PASSES `test_b04`, while moving it into a class attribute or into prose FAILS. `test_b03` counts `_dirs_to_scan` by NAME, so the shared walker must be named something else (`_child_dirs` adds 0 to the census; reusing `_dirs_to_scan` makes it 4 and fails the build) |
| 165 | **Four MORE collectors build a workspace-relative path inline, in divergent idioms that row #162's guard cannot see** -- measured iter-134: `notes.py:183` and `todos.py:314` use `str(file_path.relative_to(root))` with NO `.as_posix()` (backslashes off-POSIX) and no `ValueError` fallback, while `filesystem.py:89` and `test_posture.py:110` also omit the fallback, so an outside-root path RAISES and is swallowed to `[]` by the fail-open wrapper -- a whole collector silently empties. Row #162 single-sources only the 6 that had a named helper, by design. CHECK FIRST whether iter-139's CLI-boundary normalization (`cli.py:1232 _relative_signal_path`) already makes the posix half moot; if it does, the residual value is the missing fallback only. | Hygiene/DX | Low-Med | Low | PM-lead (iter-134) | **QUEUED -- re-scoped, the scary half is void** (iter-147): all four cited sites DO have the `ValueError` fallback and `git log -S"except ValueError"` returns one commit, the initial implementation, so nothing drifted and the iter-134 measurement was wrong. Residual: `notes.py:183` / `todos.py:314` use `str(...)` not `.as_posix()`; `test_posture.py:110` and `filesystem.py:89` are defensibly different, not duplicates. |
| 169 | **`addopts` gains `--dist worksteal`** -- warm suite 30.01s -> 26.05s (-13.2%) measured iter-141; one string in `pyproject.toml`, no dep, so `uv.lock` is untouched. TRAPS: `test_iter52_behavior.py:150` pins `addopts` EXACTLY, and the win is ONE sample on a box that measured 49.98s and 30.01s for the same command minutes apart -- needs >=3 alternating repeats before it is credible. | DX/throughput | Med | Low-Med | PM scout-B B1 (iter-141) | **QUEUED** |
| 170 | **Prune the armed self-scan gate to the 4 collectors it can fail on** -- `--fail-on-kind` is report-side only (`cli.py:780`), so 17 collectors run for a verdict only 4 can reach; `--collector` pruning cuts collect 421.6 -> ~285 ms (-29%) at 3 call sites. MUST derive the collector list FROM the armed kinds and pin the two sets equal, or a 5th armed kind goes FAIL-OPEN. | L2/throughput | Low-Med | Med | PM scout-B B2 (iter-141) | **QUEUED -- conditional on a derived-list guard; PRICED iter-147 and still not worth an iteration**: pruning saves 164.41 ms of a 423.84 ms collect (28.2% of the 582.9 ms wall), i.e. 0.16 s absolute, bought with 4 more flags on a gate command that two tests pin byte-for-byte across `make check` and `ci.yml`, plus fail-open risk if a 5th kind is armed without updating the list. The measurement stands; so does the price. |
| 171 | **The 120s BROKEN cliff is steered by a WARM number that reads ~20s low** -- measured iter-141: first-run-in-a-tree 49.98s vs 30.01s warm (1.66x), and post-release runs in a FRESH CLONE while every figure the repo cites about itself is warm. Slice = a `make test-cold` recipe plus correcting the 2-3 warm citations. An INSTRUMENT, not a speed-up; the delta is not yet attributed to bytecode. | DX/integrity | Med | Very Low | PM scout-B B3 (iter-141) | **QUEUED** |
| 172 | **Nothing detects a roadmap row that misreports its own ship** -- no `roadmap_ledger_gaps` brake exists here (the sibling `_platform` has one), so SIX rows (#156, #159, #160, #162, #166, #168) read the PRE-ship `SELECTED` while their commits were already in `git log`, none with a Done-ledger line. Repaired by hand in iter-141; the guard is what stops the recurrence. | PM maintenance/integrity | Med | Low | PM-lead (iter-141) | **QUEUED** |
| 173 | **README's intro names 4 of the 6 live providers** (omits `groq` and `together`, both fully branched and stub-tested). BLOCKED, not queued: that line sits ABOVE the `PORTFOLIO INTRO` marker, where the operator carve-out permits exactly three NUMBERS to change and a provider list is not one -- so no iteration may fix it. Recorded so a public docs-vs-code understatement is not invisible. | Docs/integrity | Low-Med | n/a | PM-lead (iter-141) | **BLOCKED -- human-owned README region** |
| 178 | **`notes.py` and `todos.py` still hand-copy the `_SKIP_DIRS` + `_is_hidden` walk policy that 10 sibling collectors import from `collectors/filesystem.py`** -- measured iter-146: an anchored `^_SKIP_DIRS` census finds 3 declarers (1 owner + those 2 holdouts) and both copies have DRIFTED, omitting `.tox`, so a note or TODO under a `.tox/` subtree is pruned by 10 collectors and scanned by 2. Fold = delete 2 frozensets + 2 byte-identical `_is_hidden`, import the seam (iter-141 `_relative` precedent). NOT purely behavior-preserving -- the `.tox` case changes for both collectors, so it needs one assertion each -- and `test_iter109_behavior.py` enumerates the local 6-member set as "the first six", so that comment is repaired in the same commit. | Hygiene/DX | Med | Low | PM scout-B B1 (iter-146) | **QUEUED -- follows #125 and merges with #118; it is a GUARD, not a fix** (iter-147): the behavior claim is falsified for the third time -- `_is_hidden` is `startswith(".")` and every prune site combines it with the set test, so `.tox` is already pruned as hidden and output is byte-identical with or without the missing entry (#118 was falsified the same way in iter-110). Honest residual: ~10 deleted lines plus an `ast` drift guard, prospective not present. Never pitch it as a `.tox` fix -- such a behavior test passes vacuously. |
| 179 | **`SPEC.md` is on the same growth curve that stalled the PM stage once already** -- 90,879 chars measured iter-146 (it was 89,249 when the operator flagged it as the watch item, so it grew rather than stabilised), the largest per-iteration required read, and it is what `cfg.vision` resolves to. The `ROADMAP.md` precedent is measured, not theoretical: at 200,022 chars it pushed the PM stage past the hard 600s cap and iterations 90/91 burned every attempt and shipped nothing. Slice = move the oldest settled module-contract prose to a `SPEC_ARCHIVE.md`, one terse line per contract, plus a char-budget guard. TRAPS: 7 test modules read `SPEC.md` as a file, so the move must be byte-preserving and those tests run FIRST; and shrinking the roadmap was necessary but NOT sufficient, so claim the payoff modestly. | PM maintenance/throughput | Med | Med-High (touches the fixed vision) | PM scout-B B3 (iter-146) | **QUEUED -- act at ~100,000 chars** |
| 182 | **The collector perception depth is not reachable by a user at all (`signals --max-items N`)** -- measured iter-150 by scout A: `todo 30` / `recent_file 20` / `git_commit 15` sit EXACTLY on their collector caps, so 3 of 8 live kinds are silently truncated, and at the cap the shipped `--baseline` ratchet is position-dependent BOTH ways (a 31st TODO sorting last is absent from `--json`; one sorting first EVICTS the tail item). BLOCKING DESIGN QUESTION: the cap lives under FOUR field names (`max_items` x14, `max_files`, `max_commits`, `max_manifests`) and `max_read_bytes` must NOT be touched, so a uniform flag mapping only `max_items` is a LIE for 2 of the 3 saturated kinds. Scope to `signals` only -- raising it for `scan` changes LLM prompt size. Its visibility half (a `capped` field on `--json`/`--summary`) touches a pinned stdout contract and is separable. | CLI/L2 | Med-High | Med | PM scout-A A1+A2 (iter-150) | **QUEUED -- settle the four-field mapping before coding** |
| 184 | **`signals --fail-over N` has had ZERO consumers since iter 145** -- iter-151: the 3 gate sites invoke only `run` and `signals` of 15 verbs, and neither `--fail-over` nor `--baseline` appears in any -- the "advertised but never demonstrated" condition the self-scan step exists to end. A count budget is also the ONLY instrument that can arm the 3 kinds `--fail-on-kind` cannot (non-zero here: red on arrival). TRAPS: `lockfile_drift` is an mtime compare, so re-measure in a FRESH CLONE before pinning N; a 4th gate line joins 3 sites pinned byte-identical by `test_iter102/110/147`. | CI/DX | Med-High | Med | PM scout-B B1 (iter-151) | **QUEUED** |
| 185 | **Every gate asserts the demo artifacts EXIST; nothing READS them back** -- `test -f .pla_runs/slate.json` + an `ls` of artifacts, while `explain`/`runs`/`trace`/`dispatch --slate` appear in ZERO gate sites, so renaming the slate `goals` key stays green and breaks the README adoption path. iter-151: `pla explain --slate ... --json` exits 0 offline, no `--goal-id`, parses as a 4-element list, so no hash need be pinned. Must assert it PARSES or it is a third existence check. | CI/DX | Med | Low-Med | PM scout-B B2 (iter-151) | **QUEUED -- consumer half only** (iter-152): #186 shipped the PRODUCER half (`run --json`), so this row may PARSE that object rather than add a third existence check |
| 187 | **Collapse iteration 142's 2 duplicated nested clean-project pytest runs into 1** -- two tests built a byte-identical one-test fixture project and differed only by `-v`, each spawning a 3-process nested pool inside the already-parallel parent; `-v` is purely additive to stdout, so one invocation satisfies both oracles verbatim (measured: rc=0, `1 passed` in stdout, `worker` x2). NOT a module-scoped fixture: that is per-worker under xdist `load`, so the saving can measure zero. | DX/throughput | Med | Low | PM scout-B B1 (iter-153) | **SHIPPED iter 153** (factory iter 159): merged on REDUNDANCY, not speed -- a paired interleaved A/B on 12 cores measured NO wall saving (36.26s two children vs 36.64s one, i.e. 0.38s SLOWER), because xdist `load` splits the children across workers so a spare worker absorbs the freed one; saving is direction-only on 2-4 core CI, unmeasured. Reasoning: iter-153 `pm.md`. |

## Done ledger (one terse line per row recorded since iter-132 -- NOT every shipped row)

Started in iter-132 to buy stage headroom (see below). A line here records only that the row SHIPPED. The verbatim detail lives in the index above while the row is still listed there, and moves to `ROADMAP_ARCHIVE.md` only when the index row is retired -- so a ledger line is NOT evidence that the archive holds the text. Measured in iter-149, these lines name rows that are still in the index and have NO archive entry: #125, #131, #149, #176, #177, #180. A count is deliberately omitted -- a hand-maintained denominator over an append-only ledger decays on the very next ship, with no oracle to catch it. Verify archive coverage per row before deleting anything.

- #121 Close the deferred disallow_any_generics flag: the 35 bare-generic type-arg sites (iter 139, factory iter 146)
- #125 Hoist the duplicated _has_source + _SOURCE_EXTS into collectors/filesystem.py, deleting license.py copy and the hand-sync comment (iter 147, factory iter 153)
- #128 Publish the state-dir artifact layout, guarded against the 4 cli.py fi... (iter 124, factory iter 131, d8ed387)
- #131 Bump the README intro's stale tests floor 2,700+ -> 3,300+, closed by row #160's oracle commit (iter 136, factory iter 143, 48378fa)
- #134 Finish the atomic-write idiom: Checkpoint.save temp-file cleanup + the... (iter 127, factory iter 134, 4e48072)
- #139 Collapse the 16 hand-copied never-raises collect() wrappers into one t... (iter 122, factory iter 129, 5393347)
- #141 Memoize todos' per-line extraction on a content digest (iter 123, factory iter 130, 1a80355)
- #145 Dogfood the enforcement gate: arm a signals --fail-on-kind self-scan a... (iter 121, factory iter 128, 6af2321)
- #147 Extend the in-table-blank-line guard to ROADMAP_ARCHIVE.md, which has... (iter 126, factory iter 133, ebc4ff3)
- #148 signals --exclude-path GLOB (iter 125, factory iter 132, 808eb16)
- #149 Ancestor matching for --exclude-path: a bare directory name now means the subtree (iter 149, factory iter 155)
- #150 signals --baseline FILE (iter 131, factory iter 138, 59c4032)
- #152 Make the bundled offline driver drive a 2-tick watch, so the documente... (iter 128, factory iter 135, 9363fae)
- #153 One shared per-scan text provider so the 3 content collectors stop re-... (iter 129, factory iter 136, 0d00b5d)
- #154 Collapse working_tree's 2 git spawns per scanned directory into 1: tak... (iter 130, factory iter 137, 17fae45)
- #156 BrokenDocLinkCollector: the 17th collector, 2nd relational perceiver (iter 137, factory iter 144, 5939593)
- #157 Normalize ContextSignal.path to ONE workspace-relative namespace at the... (iter 132, factory iter 139, a7be482)
- #159 Ship the opt-in hooks/pre-commit gate the README advertised twice (iter 133, factory iter 140, 86a0d7f)
- #160 Make the README suite-size claim a two-sided oracle (iter 136, factory iter 143, 48378fa)
- #162 Host the one _relative on BaseCollector, delete the 6 copies (iter 134, factory iter 141, e368632)
- #164 Fold the 3 OpenAI-wire _complete closures into the _SdkAdapter seam (iter 141, factory iter 148)
- #166 Parallelize the suite: pytest-xdist + addopts -n auto (iter 135, factory iter 142, 6fac577)
- #167 Arm broken_link in the self-scan gate: the 4th armed kind, all 3 sites (iter 140, factory iter 147)
- #168 signals --fail-over N: a count-budget exit gate (iter 138, factory iter 145, 004f037)
- #174 Pin -n 2 on the 2 nested pytest children so they stop nesting 12 xdist workers (iter 142, factory iter 149)
- #175 Hotfix: b08 determinism test compares two runs over a tmp_path fixture copy (iter 143, factory iter 150)
- #176 Close the other 4 shared-mutable-tree comparisons; census now sees helper-indirect runs (iter 144, factory iter 151)
- #177 Publish the exit-code contract on pla --help; exit 5 now names both gates (iter 146, factory iter 152)
- #180 Correct the 2 false _dirs_to_scan parity docstrings; pin the 2 walk flavors with an ast census (iter 148, factory iter 154)
- #181 make check-matrix: both CI matrix legs locally, per-leg UV_PROJECT_ENVIRONMENT, leg set pinned to ci.yml (iter 150, factory iter 156)
- #183 A repeated signals --kind silently kept only the last value; now a parse-time exit-2 usage error (iter 151, factory iter 157)
- #186 run --json: one JSON object on stdout, human rendering to stderr; dispatch/resume untouched (iter 152, factory iter 158)
- #187 Merge iter-142's 2 nested clean-project pytest runs into 1: redundancy removed, no measured wall saving (iter 153, factory iter 159)

_Roadmap owned by the PM-lead role; updated each iteration (mark shipped, re-order on learnings)._
