# Enhancement Roadmap

A maturity/enhancement backlog layered on the shipped **v0.1.1**, grounded in
[`SPEC.md`](SPEC.md). The product is already complete and tested (134 tests
green through iter-02); everything here is a small, self-contained increment
that raises the bar of an existing layer — L2 scout perception, L1 loop
execution, L0 resilience/observability, or CLI/DX — without breaking the public
contracts. Each iteration of the dev loop selects exactly one row.

Ordering is by leverage on the product's thesis ("a proactivity layer that is
resilient by design"), balanced against blast radius, with one standing
tie-break: an integrity fix (a live docs-vs-code contradiction on a public
repo) outranks a net-new capability. Per-row reasoning lives in that
iteration's `pm.md`; the full verbatim history is in
[`ROADMAP_ARCHIVE.md`](ROADMAP_ARCHIVE.md).

> **This file is an INDEX, deliberately kept small.** It grew to 200KB of settled history, which pushed the PM stage past the agent CLI's hard 600-second timeout and stalled the loop entirely (iteration 90 lost all four attempts, producing nothing).
>
> Full rationale for every row -- the complete status prose and the iteration-by-iteration narrative -- is preserved verbatim in [`ROADMAP_ARCHIVE.md`](ROADMAP_ARCHIVE.md). Read that file ONLY when you need one specific row's detail; never read it wholesale.
>
> **Maintainers (including automated contributors): keep rows one line and terse.** Put a new row's full reasoning in your iteration's `pm.md`, not here. If this file passes ~40KB again the loop will stall the same way.

_Settled rows are dropped from this index once shipped -- rows 1-70 in iter-101, rows 71-98 in iter-116, and 28 of rows 99-145 in iter-123 -- after per-row verification that each one is present in [`ROADMAP_ARCHIVE.md`](ROADMAP_ARCHIVE.md), which holds their full verbatim text. **The archive now covers rows 1-98, the 28 settled rows of 99-145 moved in iter-123, and the 11 shipped rows moved in iter-132 (#128, #134, #139, #141, #145, #147, #148, #150, #152, #153, #154 -- each verified ABSENT from the archive before the move and present after); anything still listed below exists ONLY here, so never drop a row without first confirming the archive has it.** Shipped rows keep a one-line record in the Done ledger at the foot of this file, so `grep` still answers "did we ship that?" without opening the archive. Look up a single row by number; never read that file wholesale._

| # | Enhancement | Layer | Value | Risk | Source | Status |
|---|-------------|-------|-------|------|--------|--------|
| 105 | **`DebugArtifactCollector` (`kind="debug_artifact"`)** -- AST-parse-only collector for leftover debug scaffolding (would be the 17th collector). | L2 | Med | Low | PM-lead (iter-98) | **ABANDONED -- iter-98** (spec'd + engineered, never reached tester/final; no commit -- commit-seq 105 is skipped; re-proposable as-is) |
| 109 | **Char-size budget test for the per-iteration required-reading docs** -- `ROADMAP.md` (~40KB operator trigger) and `SPEC.md` (90,269 chars) have NO size guard, and the 200KB-roadmap stall cost iters 90-91 all 8 attempts and 0 commits. Must prove itself against a synthetic over-budget file, and must NOT rewrite `SPEC.md` (operator-owned). | DX/integrity | Med | Very Low | PM scout-B B3 (iter-100) | **QUEUED** |
| 117 | **Packaging-contract oracle** -- `[project.scripts] pla = "proactive_loop.cli:main"` and the `py.typed` marker are asserted by ZERO oracles (CI and every local run use the SOURCE tree), so renaming `main` or dropping the marker from the wheel stays green. Must stay OFFLINE: assert the declared entry point resolves to a real callable + the marker is in the built package file list; a real `pip install` needs network and is FORBIDDEN. Shrinks to entry-point resolution alone if the marker already ships. | Packaging/integrity | Med | Low | PM scout-A A3 (iter-104) | **QUEUED** |
| 118 | **Consolidate the 3 divergent `_SKIP_DIRS`/`_is_hidden` definitions** -- `filesystem.py` has 7 entries, `notes.py`/`todos.py` 6 each (both missing `.tox`); 13 use sites, and `loop/tools.py` (L1 ACT sandbox) imports a private name from an L2 leaf collector. CAVEAT measured iter-104 by scout B: the divergence is behaviorally INERT (all missing entries are hidden names and every site ANDs `not _is_hidden(d)`), so the diff is cosmetic and stands ONLY on an `ast` single-definition drift guard. | Hygiene/DX | Low-Med | Low | PM scout-B B1 (iter-104) | **QUEUED (cosmetic -- guard is the only value)**; re-proposed iter-110 as a user-visible `.tox` leak and FALSIFIED AT RUNTIME by the PM lead: `_is_hidden` is `startswith('.')` and all 3 walk sites AND it, so a live probe (TODO + note planted under `.tox/`) emitted ZERO `.tox` signals -- the proposed behavior test would PASS unchanged, i.e. vacuous. Iter-104's INERT caveat stands. |
| 121 | **Close the deferred `disallow_any_generics` flag: the 35 bare-generic (`type-arg`) sites** -- measured iter-108 at `2bd44f7`, the per-file split is `cli.py` 19, `loop/tools.py` 15, `loop/executor.py` 1. Mechanical (`dict` -> `dict[str, X]`) but it touches the two hottest files, so it is its own iteration. Flip the flag to `true` in the SAME commit or the ratchet is not actually closed. | Typing/hygiene | Med | Low-Med | PM-lead (iter-108, deferred half of row #120) | **QUEUED** (engineered + reviewer-APPROVED in iter-114 and LOST to a tester-stage stall -- four 166-byte "Connection stalled" attempt logs, no `final.md`, tree reverted -- so it is re-proposable with its design risk already burned down; precedent: row #94) |
| 122 | **`PythonVersionDriftCollector` (`kind="python_version_drift"`)** -- would be the 17th collector and only the 2nd RELATIONAL one (`lockfile_drift` is the sole one today): `requires-python`, an optional `.python-version`, and the CI matrix floor are three independently-edited declarations of ONE fact. `tomllib` is stdlib, so NO new dep and `uv.lock` is untouched. CAVEAT measured iter-108: parsing an Actions matrix without PyYAML is a heuristic line scan, so it must emit ONLY when two floors both parse unambiguously AND disagree -- a false drift signal on a stranger's repo is worse than silence. Silent on this repo (`>=3.12` vs matrix floor 3.12, no `.python-version`), so `make demo` is unaffected. | L2 | Med-High | Low-Med | PM scout-A A2 (iter-108) | **QUEUED** |
| 125 | **Delete the verbatim second copy of `_has_source` + `_SOURCE_EXTS` (`license.py:99-114`/`58` duplicates `ci_config.py:87-101`/`46`), including the hand-written keep-in-sync comment at `license.py:56-57`** -- logic-identical bodies (a raw `ast.dump` diff reports them different only because of docstring prose), so zero behavior risk; value is deleting ~17 lines plus a comment that asks a human to keep two constants equal by hand. PREMISE CORRECTED iter-117 (scout-A A2): the constant exists in THREE places, not two -- `ci_config.py:46` + `license.py:58` (`_SOURCE_EXTS`) + `test_posture.py:38` (`_CANDIDATE_EXTS`, which is why the row missed it), so search a duplicated constant by VALUE, never by name; folding `test_posture` in couples two questions that are merely equal today, so the fallback scope is 2 copies + a comment naming the third. OPEN DESIGN QUESTION the engineer must settle first: hoisting into `collectors/filesystem.py` (which both files already import `_SKIP_DIRS`/`_is_hidden` from) makes it a grab-bag utility module, vs. a new `collectors/_walk.py` which turns a deletion into a new module. | Hygiene/DX | Low-Med | Low | PM scout-B B2 (iter-110) | **QUEUED** |
| 129 | **File-level cheap prefilter before the per-line regex loop in `todos` (and `merge_conflict`)** -- measured iter-112 via the shipped `--timings`: a scan is 433.1 ms, of which `todos` 128.8 (30%) and the per-line pass is nearly all of it (52.1 ms of a 70 ms warm `_collect` is `splitlines` + 2 regex calls per line). CAVEAT the engineer must respect: a wrong prefilter SILENTLY DROPS signals in the L2 perception layer, and `[\s]` admits a TAB so a `"[ ]"` substring prefilter is UNSOUND -- the prefilter must be provably weaker than the regexes. Second caveat: durations are barred from every stdout contract here, so the suite can only assert byte-identical output, i.e. it cannot verify the win; whoever ships it must measure it at the gate. | L2/throughput | Med | Low-Med | PM scout-A A1 (iter-112), deferred vs the row #126 integrity fix | **QUEUED** (SHAPE SETTLED by scout-A measurement iter-124, memo bypassed, 190 files/68,906 lines: the sound `text.lower()`+substring prefilter nets **+18.78 ms** (53.99 -> 35.21 ms), a single whole-buffer `re.IGNORECASE` prefilter is a measured net **LOSS of -6.66 ms** (the prefilter alone costs 37.19 ms, 69% of the loop it skips), and a case-SENSITIVE prefilter drops **8 files on this repo**. Residual unsoundness to close in the diff: a `str` pattern's `\s` is UNICODE (29 codepoints), so pin the derived set as a constant and RE-DERIVE it in a test) |
| 131 | **Bump the README intro's stale `tests` floor (published `2,200+`, live 2,591)** -- measured iter-112 at `a479e02`: `uv run pytest` = 2591 passed, while both intro claims (README L13, L19) still say `2,200+ tests`, understating the suite by ~15%. The claim is TRUE (it is a floor) and `test_readme_and_ci_contract.py` only asserts the `+`, so NO guard currently fires -- which is why this needs an explicit row. This is one of exactly three numbers the operator carve-out permits an automated contributor to change inside the human-owned PORTFOLIO INTRO block; change ONLY the digits, in both places, and nothing else above the marker. | Docs/integrity | Low-Med | Very Low | PM-lead (iter-112), out of scope of row #126 to keep the above-marker byte-identity assertion strict | **QUEUED** (RE-MEASURED iter-125 by PM scout-A at `d8ed387`: live collection is **2,986** across 145 test files, and the floor was hand-bumped `2,200+` -> `2,700+` at `ac917e6` so it was ALREADY ~150 stale on arrival. Second, separable defect found the same run: the existing guard `test_readme_and_ci_contract.py::test_readme_states_the_suite_size_as_a_floor_not_an_exact_count` only asserts a trailing `+` and `> 0`, so it passes just as happily on a FALSE `9,000+` -- it is fail-open in both directions. A static `ast` count of `test_*` functions is 2,239 (BELOW the published floor, because parametrize expands at collection) so it can NEVER prove the claim; only a real `--collect-only` can, measured 0.61-0.72s warm. CAUTION that deferred it in iter-125: any staleness CEILING manufactures recurring red builds on a public repo at ~+33 tests/commit, so pick K deliberately) |
| 136 | **Oracle the 2 unguarded `_out_dir_guard` messages** -- measured iter-116: no test file contains `--out-dir is not a directory` or `--out-dir parent`, and no test names `_out_dir_guard`, so the NEWEST of the 3 CLI path guards (row #132) is the least guarded while `test_iter44_behavior.py` pins the other 3 messages verbatim. | CLI/test | Low-Med | Very Low | PM scout-B B1 (iter-116), separated from its guard-collapse by PM-lead | **QUEUED** |
| 137 | **Put the `watch` -> `diff` change feed inside the graded gate (`make check` + `ci.yml`)** -- the 2 newest features are exercised by NO gate step. PREREQUISITE (measured iter-116): `examples/scripted_responses.json` has ONE `synthesize` of 7 entries and `ScriptedLLMClient` raises `ScriptExhaustedError` in order, so a 2-tick watch exhausts on tick 2 and leaves nothing to diff -- add a 2nd `synthesize` FIRST or it is fail-open gate theatre. `test_iter102_behavior.py` pins `make check` to `ci.yml` (same commit). | CI/DX | Med-High | Med | PM scout-A A2 (iter-116) | **QUEUED -- gate-step half only** (the measured prerequisite ships separately as row #152 in iter-128, factory iter 135, so what remains here is the `make check` + `ci.yml` step itself plus the positional `ARTIFACT_ASSERTION_STEPS` re-bounding) |
| 138 | **Char-size budget guard for `ROADMAP.md`** -- iter-116 cut rows 71-98 (36,203 -> 28,899 chars, ~20 iterations of headroom), but nothing stops re-growth, and this file has killed the loop once (iters 90-91, 0 commits/9h). RE-MEASURED iter-121 (git show of each sha, piped to wc -c): 28,947 -> 30,672 -> 34,210 -> 35,222 -> 36,381 = **+1,859 chars/iteration**, so iter-116's "~20 iterations" was ~5x optimistic and only ~2 iterations of headroom remain under the operator's 40,000 threshold. Pairs with row #109. TRAP for any future cut: `ROADMAP_ARCHIVE.md` holds rows 1-98 ONLY, so rows 99+ exist solely here -- verify archive coverage per-row before deleting anything. | PM maintenance | Med | Very Low | PM scout-B B3 (iter-116) | **QUEUED** (RE-MEASURED iter-123: the index reached 38,074 chars, confirming +1,859/iter, so the PM-lead compacted the 28 settled rows of 99-145 into the archive as maintenance in the same commit -> 17,458 chars, ~11 iterations of headroom; 3rd compaction after iter-101 and iter-116. The GUARD itself is still unbuilt, which is why this keeps recurring) |
| 142 | **SPEC QUESTION FIRST: may `pla signals` write a cross-process cache?** -- row #130's memo dies at process exit, but the dominant invocation is a FRESH process (`pla signals`, `make demo`, every CI step, tick 1 of every `watch`), which measured iter-118 pays 179.7-209.9 ms of a 341-386 ms scan versus 21.0-25.1 ms warm; a persisted digest->verdict map would cut a one-shot scan ~45%. BLOCKED, not queued: `SPEC.md:846` specifies `pla signals` as "read-only, LLM-free" and `state_dir` appears ZERO times under `collectors/`, so this needs an operator spec amendment plus L2-boundary plumbing, a cache-version key, a size cap and corruption tolerance. Decide the clause before writing code. | L2/throughput | High | Med-High | PM scout-A A3 (iter-118) | **BLOCKED -- needs an operator SPEC decision** |
| 143 | **Measured DEAD, recorded so nobody re-derives them** (iter-118 instrumentation, do not re-propose): `merge_conflict` digest memo nets **+1.09 ms** (whole scan pass is 4.51 ms vs a 3.42 ms digest; its 26.4 ms warm cost is walk+stat+read); `ast.parse` / `PyCF_ONLY_AST` instead of `compile(exec)` is **indistinguishable** (145.93 / 145.51 / 145.95 ms over the same 174-file corpus, error attributes identical); collapsing the 10 full-tree walks into one caps at **~8.6 ms, 2.4%** (the pruned tree is 14 dirs / 190 files); git spawns are only **3 per scan**, root-only. METHOD TRAP: an `os.walk` instrumentation wrapper must be a GENERATOR passthrough, or `dirnames[:]` pruning is defeated and every count inflates ~8x. | L2/throughput | n/a | n/a | PM scout-A (iter-118) | **CLOSED -- measured not worth an iteration** |
| 146 | **Extract the bounded digest-memo mechanism into ONE shared helper** -- iter-123 leaves TWO near-identical module-level memos (`syntax_error._PARSE_MEMO`, `todos`' item memo): each has a cap constant, FIFO eviction, a `clear_*()` and a `*_stats()` seam, ~40 lines apiece. Deliberately NOT bundled into iter-123: the shipped `syntax_error` seam carries 11 assertions bound to its exact names, including a monkeypatch of `PARSE_MEMO_MAX_ENTRIES` proving the cap is read at RUNTIME, so a shared class must late-bind that cap or the oracle breaks. Two instances is the minimum needed to see the shape. | Hygiene/DX | Low-Med | Low-Med | PM-lead (iter-123, deferred half of row #141) | **QUEUED** |
| 149 | **Ancestor-directory matching for `--exclude-path`** -- row #148 ships ONE rule (pattern vs the whole relpath, `*` crossing `/`), which leaves two warts measured iter-125: `--exclude-path 'src/*'` does NOT drop the `test_posture -> src` signal (a bare DIRECTORY path), and an any-depth exclusion needs a leading `*` (`'*node_modules/*'`). Matching the pattern against every ancestor prefix too would make `--exclude-path src` mean the subtree, gitignore-style. Deliberately deferred to keep #148 one rule, one sentence, one oracle. | CLI/DX | Low-Med | Low | PM-lead (iter-125, deferred half of row #148) | **QUEUED** |
| 151 | **Make `resume` tolerant of a corrupt `meta.json` the way the run LISTING already is** -- `_read_meta` (`cli.py:1698-1701`) is a bare `json.loads`, and `_cmd_resume` (`cli.py:3289`) calls it UNWRAPPED while `_run_row` (`cli.py:1744-1747`) wraps the identical call in `except (ValueError, OSError)` and degrades to `{}`, so the SAME corrupt file is tolerated by `runs` and fatal to `resume` (one `error:` line, exit 1) even when the checkpoint beside it is intact. Iter-127 shipped the WRITE side (row #134) and deliberately left this: the open design question is WHICH degrade is right, because silent `{}` inherits `_cmd_resume`'s `Path(".")` fallback and would resume against the CWD as if it were the workspace -- so the honest shape is probably a named error that names the file, or a `--workspace` override on `resume`, NOT copying the listing's silence. README:318 also lists only a malformed slate or `checkpoint.json` under exit 1, never `meta.json`. | L0/durability | Low-Med | Low | PM-lead (iter-127, deferred half of row #134) | **QUEUED** |
| 155 | **Root-Markdown table guard audits the WORKING DIR, not the repo** -- `test_iter133_behavior.py:404` globs `REPO_ROOT` (7 files here) where git tracks 5, so its verdict depends on untracked scratch files; and its anti-vacuous floor names only 3 files, so tracked public `SPEC.md`/`DIRECTIONS.md` can leave the audited set and still pass. Row #147's status names 2 untracked files, so no clone reproduces it. Fix = key on the TRACKED set, assert set EQUALITY. | DX/integrity | Med | Very Low | PM scout-A A1 (iter-131) | **QUEUED** (front-runner; rated strongest alternative to #150 in iter-131) |
| 156 | **`BrokenDocLinkCollector` (`kind="broken_link"`)** -- 17th collector, 2nd RELATIONAL one: a relative Markdown link the filesystem disproves. Stdlib only, no new dep. Measured iter-131: 7 tracked `.md`, 7 links, 0 broken, 0 inside fenced blocks -- so its correct value is state-independently ZERO and it can join row #145's armed set on day one. HARD REQUIREMENT: skip fenced blocks/inline code and ignore `http(s)`/`mailto`/anchor targets, or a code sample reddens the PUBLIC build. | L2 | Med | Low-Med | PM scout-B B2 (iter-131) | **QUEUED** |
| 158 | **A re-rooted `--workspace SUB` INVENTS repo-posture findings and leaves the git kinds unscoped** -- measured iter-132 by scout A: under `--workspace src`, `ci_config` emits "no CI configured" and `license` emits "no license file" although `.github/workflows/ci.yml` and `LICENSE` both exist at the root (`license` does not appear in the root scan at all, so re-rooting INVENTS a kind), while `git_commit` stays at full-repo 15 because git discovers the enclosing repo and only its path degrades to the literal workspace string. These are exactly the kinds a user would arm in `--fail-on-kind`. Distinct from #157, which is path SPELLING, not perception SCOPE -- do not bundle them. | L2 | Med | Med | PM scout-A PM-feedback (iter-132) | **QUEUED** |
| 159 | **Ship the `hooks/pre-commit` gate the README already advertises twice** -- README:166 and :358 both sell exit 5 as "the channel a pre-commit hook or a CI step branches on", yet there is no `hooks/` dir and no `.pre-commit-config.yaml` in the tree, so half of a twice-published claim has never existed. Opt-in POSIX-sh hook running the CI-identical armed set (`merge_conflict`/`syntax_error`/`secret_file`, measured state-independent at `Makefile:99` + `ci.yml:84`), CLI exit status passed through unchanged, fail-CLOSED when neither `pla` nor `uv` resolves, plus the one-line `git config core.hooksPath hooks` install. No `src/`, no dependency, no new gate step. | CLI/DX | Med | Very Low | PM scout-B B3 (iter-133) | **SELECTED -- iter-133** |
| 160 | **The README suite-size guard is fail-open in BOTH directions** -- `test_readme_and_ci_contract.py:246` asserts only a trailing `+` and `floor > 0`, so a FALSE `9,000+` passes as happily as the stale `2,700+`. Measured iter-133: live collection is 3,211 tests across 153 files, so the published floor understates by 511 (~16%). The sound fix is ONE-SIDED (`live >= published`) from a real `--collect-only` subprocess -- a static `ast` count is 2,239 (below the floor, since parametrize expands at collection) and can never prove the claim, while a staleness CEILING would redden a public build at ~+33 tests/commit. Cost to weigh first: a 153-module collection subprocess (1.21s) inside a suite already at 82.42s. | DX/integrity | Med | Low | PM scout-A A1 (iter-133) | **QUEUED** |
| 161 | **Give `--baseline` its first consumer: commit a snapshot and arm `ci_config` only** -- the ratchet shipped as row #150 and nothing consults it; the Makefile's "red on arrival" reasoning for the 3 unarmed kinds is now stale. Measured iter-133: of those three only `ci_config` is safely armable (stable identity, and the no-CI case emits a DIFFERENT summary, so a baselined entry becomes a real "the workflow was deleted or renamed" detector); `test_posture` churns BY CONSTRUCTION (its count is inside `summary`, one of the 6 identity keys) so arming it is a self-inflicted public red build; `lockfile_drift` is mtime-driven, baseline-safe but worthless as enforcement. Needs a stated refresh rule or the committed snapshot rots into a blindfold. | L2/enforcement | Med | Med | PM scout-B B2 (iter-133) | **QUEUED** |

## Done ledger (shipped rows whose verbatim detail now lives in `ROADMAP_ARCHIVE.md`)

Moved out of the index in iter-132 to buy stage headroom (see below). One line each; look the row up by number in the archive for the full text.

- #128 Publish the state-dir artifact layout, guarded against the 4 cli.py fi... (iter 124, factory iter 131, d8ed387)
- #134 Finish the atomic-write idiom: Checkpoint.save temp-file cleanup + the... (iter 127, factory iter 134, 4e48072)
- #139 Collapse the 16 hand-copied never-raises collect() wrappers into one t... (iter 122, factory iter 129, 5393347)
- #141 Memoize todos' per-line extraction on a content digest (iter 123, factory iter 130, 1a80355)
- #145 Dogfood the enforcement gate: arm a signals --fail-on-kind self-scan a... (iter 121, factory iter 128, 6af2321)
- #147 Extend the in-table-blank-line guard to ROADMAP_ARCHIVE.md, which has... (iter 126, factory iter 133, ebc4ff3)
- #148 signals --exclude-path GLOB (iter 125, factory iter 132, 808eb16)
- #150 signals --baseline FILE (iter 131, factory iter 138, 59c4032)
- #152 Make the bundled offline driver drive a 2-tick watch, so the documente... (iter 128, factory iter 135, 9363fae)
- #153 One shared per-scan text provider so the 3 content collectors stop re-... (iter 129, factory iter 136, 0d00b5d)
- #154 Collapse working_tree's 2 git spawns per scanned directory into 1: tak... (iter 130, factory iter 137, 17fae45)
- #157 Normalize ContextSignal.path to ONE workspace-relative namespace at the... (iter 132, factory iter 139, a7be482)

_Roadmap owned by the PM-lead role; updated each iteration (mark shipped, re-order on learnings)._
