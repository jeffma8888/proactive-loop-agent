# fixture_workspace

A small, fake personal workspace used by the demo and integration tests. It
contains a couple of learning/side projects and a working journal so the
collectors have realistic recent files, TODO/FIXME comments, and notes to scan.

There is deliberately **no** git repository inside this directory, so the git
collector degrades to an empty result on a clean checkout.
